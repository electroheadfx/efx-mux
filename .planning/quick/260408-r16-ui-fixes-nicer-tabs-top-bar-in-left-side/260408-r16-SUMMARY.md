---
status: complete
---

# Quick Task 260408-r16: UI fixes - Summary

## Changes

### Task 1: Polish tab bar styling and file viewer close button
- **tab-bar.tsx**: Tab container uses `gap-1 pt-1.5` for better spacing. Active tabs get rounded-top corners (`rounded-t`), side/top borders with no bottom border (`border border-border border-b-0`), raised background, and 150ms transition. Inactive tabs get transparent borders with `hover:text-text-bright hover:bg-bg-raised/50` transition.
- **main-panel.tsx**: Close button gets larger padding (`px-2.5 py-1`), rounded corners, and hover state with accent border (`hover:bg-bg hover:text-text-bright hover:border-accent`) and 150ms transition.

### Task 2: Add global custom scrollbars and fix panel scroll
- **app.css**: Added global `*::-webkit-scrollbar` rules (8px width/height, transparent track, border-colored thumb with 4px radius, text-colored thumb on hover). Keeps xterm-specific rules with bg-colored track.
- **app.css**: Fixed `.right-top-content, .right-bottom-content` overflow from `hidden` to `overflow-y: auto` to restore mouse wheel scrolling in right panel viewers (GSD, Diff, File Tree).

## Commits
- `4156cc1` fix(260408-r16): polish tab bar styling and file viewer close button
- `981d54b` fix(260408-r16): add global custom scrollbars and fix panel scroll
