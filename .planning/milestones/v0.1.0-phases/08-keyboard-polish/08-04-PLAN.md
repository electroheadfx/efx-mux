---
phase: 08-keyboard-polish
plan: 04
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/main.tsx
  - src/components/fuzzy-search.tsx
autonomous: true
gap_closure: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "Ctrl+P opens fuzzy search overlay when pressed from any context"
    - "Ctrl+P still works when terminal is focused (capture-phase fires before xterm.js)"
    - "Fuzzy search Escape/ArrowDown/ArrowUp/Enter still work after fix"
  artifacts:
    - path: "src/main.tsx"
      provides: "Ctrl+P dispatches open-fuzzy-search CustomEvent"
      contains: "new CustomEvent('open-fuzzy-search')"
    - path: "src/components/fuzzy-search.tsx"
      provides: "Receives open-fuzzy-search event and opens search overlay"
      contains: "addEventListener('open-fuzzy-search'"
  key_links:
    - from: "src/main.tsx"
      to: "src/components/fuzzy-search.tsx"
      via: "CustomEvent('open-fuzzy-search') dispatch -> listener"
      pattern: "dispatchEvent.*open-fuzzy-search"
---

<objective>
Fix broken Ctrl+P fuzzy search after keyboard consolidation in Plan 01.

Purpose: The consolidated capture-phase keyboard handler in main.tsx intercepts Ctrl+P with `e.preventDefault()` + `e.stopPropagation()` but does not actually trigger the fuzzy search. The fuzzy-search module's bubble-phase listener never fires because stopPropagation() in capture phase blocks all further propagation. This is the sole code gap identified in 08-VERIFICATION.md.

Output: Ctrl+P opens fuzzy search from any context (terminal focused or not).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-keyboard-polish/08-VERIFICATION.md

<interfaces>
From src/components/fuzzy-search.tsx:
```typescript
// Line 121 - already listens for this CustomEvent:
document.addEventListener('open-fuzzy-search', openSearch);

// Line 30 - openSearch is NOT exported (module-private):
function openSearch() { ... }
```

From src/main.tsx (lines 139-142 - the broken handler):
```typescript
case key === 'p' && !e.shiftKey && !e.altKey:
  e.preventDefault(); e.stopPropagation();
  // fuzzy search is handled by its own module-scope listener
  break;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Ctrl+P to dispatch open-fuzzy-search CustomEvent</name>
  <files>src/main.tsx, src/components/fuzzy-search.tsx</files>
  <read_first>
    - src/main.tsx (lines 139-142 for the Ctrl+P case; line 18 for fuzzy-search import)
    - src/components/fuzzy-search.tsx (lines 118-121 for the existing event listeners)
  </read_first>
  <action>
In `src/main.tsx`, replace the comment-only Ctrl+P case (lines 139-142) with an actual dispatch:

```typescript
case key === 'p' && !e.shiftKey && !e.altKey:
  e.preventDefault(); e.stopPropagation();
  document.dispatchEvent(new CustomEvent('open-fuzzy-search'));
  break;
```

This works because `fuzzy-search.tsx` line 121 already has:
```typescript
document.addEventListener('open-fuzzy-search', openSearch);
```

The CustomEvent is a new event dispatch (not the same KeyboardEvent), so it bypasses the stopPropagation() barrier.

Additionally, in `src/components/fuzzy-search.tsx`, remove the now-redundant Ctrl+P detection from the `handleGlobalKeydown` function (lines 88-92). The capture-phase handler in main.tsx now exclusively owns Ctrl+P interception. The handleGlobalKeydown function should KEEP the Escape/ArrowDown/ArrowUp/Enter handling (lines 93-115) -- only remove the `if (e.ctrlKey && e.key === 'p')` block at lines 88-92 since that code path is unreachable after the capture-phase handler calls stopPropagation().

After removal, `handleGlobalKeydown` should start with `if (!visible.value) return;` (currently line 93).
  </action>
  <verify>
    <automated>grep -n "CustomEvent('open-fuzzy-search')" /Users/lmarques/Dev/efx-mux/src/main.tsx && grep -n "addEventListener('open-fuzzy-search'" /Users/lmarques/Dev/efx-mux/src/components/fuzzy-search.tsx && ! grep -n "e.ctrlKey && e.key === 'p'" /Users/lmarques/Dev/efx-mux/src/components/fuzzy-search.tsx && echo "PASS: Ctrl+P wired via CustomEvent, redundant handler removed"</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx Ctrl+P case contains `document.dispatchEvent(new CustomEvent('open-fuzzy-search'))`
    - src/main.tsx Ctrl+P case does NOT contain the comment "fuzzy search is handled by its own module-scope listener" as the only action
    - src/components/fuzzy-search.tsx line 121 still contains `document.addEventListener('open-fuzzy-search', openSearch)`
    - src/components/fuzzy-search.tsx handleGlobalKeydown does NOT contain `e.ctrlKey && e.key === 'p'` (redundant path removed)
    - src/components/fuzzy-search.tsx handleGlobalKeydown still contains Escape, ArrowDown, ArrowUp, Enter handlers
    - `pnpm build` succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Ctrl+P in the consolidated capture-phase handler dispatches a CustomEvent that opens the fuzzy search overlay. The redundant bubble-phase Ctrl+P detection in fuzzy-search.tsx is removed. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input -> app handler | User keystrokes processed by capture-phase handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | CustomEvent dispatch | accept | CustomEvent('open-fuzzy-search') is an internal dispatch within the same document context; no external input vector. openSearch() only toggles a visibility signal. |
</threat_model>

<verification>
1. `pnpm build` passes with no errors
2. `grep -n "CustomEvent('open-fuzzy-search')" src/main.tsx` returns a match
3. `grep -n "e.ctrlKey && e.key === 'p'" src/components/fuzzy-search.tsx` returns NO match
</verification>

<success_criteria>
Ctrl+P keyboard shortcut dispatches CustomEvent that triggers fuzzy search open. The gap identified in 08-VERIFICATION.md (Ctrl+P does nothing after keyboard consolidation) is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-keyboard-polish/08-04-SUMMARY.md`
</output>
