---
phase: 10
plan: '06'
type: execute
wave: 6
depends_on: ['10-01', '10-02', '10-03', '10-04', '10-05']
files_modified:
  - src/components/main-panel.tsx
  - src/components/right-panel.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "main-panel.tsx uses bgDeep terminal area, updated TerminalTabBar styling, file viewer overlay with navy-blue palette"
    - "right-panel.tsx uses reference composition (tab bar + content + bash panel), bgDeep content areas"
    - "All child component mounts (AgentHeader, TerminalTabBar, ServerPane) preserved exactly"
  artifacts:
    - path: "src/components/main-panel.tsx"
      provides: "Main panel with terminal area, agent header, file viewer overlay, server pane integration"
      contains: "bg-bg-terminal"
    - path: "src/components/right-panel.tsx"
      provides: "Right panel with GSD/Diff/FileTree routing and bash terminal"
      contains: "bgDeep"
---

<objective>
Rewrite main-panel.tsx and right-panel.tsx container components. These components compose child components (TerminalTabBar, AgentHeader, ServerPane, GSDViewer, DiffViewer, FileTree) and manage layout. Their existing application logic (event listeners, state signals, child component mounting) must be preserved while updating visual markup to use the navy-blue palette.

This plan has a checkpoint:human-verify at the end because visual correctness of these complex compositions requires human inspection.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/main-panel.tsx (current — understand file viewer, terminal-containers, server pane)
@src/components/right-panel.tsx (current — understand tab switching, bash terminal)
@RESEARCH/theme/MainPanel.tsx (reference composition)
@RESEARCH/theme/RightPanel.tsx (reference composition)
@src/components/agent-header.tsx (already rewritten in Plan 02)
@src/components/server-pane.tsx (complex state machine — update carefully)

# Reference MainPanel composition
- TerminalTabBar: bgBase background, bottom border bgBorder, pill-style tabs
- AgentHeader: bgElevated fill, 8px radius, 8px 12px padding, gradient icon, status pill
- Terminal area: bgDeep, full height minus tab bar and agent header
- terminal-containers: absolute inset-0 (critical for xterm.js mount)
- File viewer overlay: bgBase fill, bg-raised header bar, READ-ONLY badge, Close button

# Reference RightPanel composition
- Tab bar: bgBase, borderBottom bgBorder, pill buttons (bgElevated + borderSurface for active)
- Content areas: bgDeep
- Split handle: horizontal, bgBorder color
- Bash panel: 200px height, bgBase, BashTabBar + BashContent

# Critical: terminal-containers must remain
The div.terminal-containers.absolute.inset-0 is the mount point for xterm.js instances managed by terminal-tabs.tsx. Its position and structure MUST NOT be changed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite main-panel.tsx with navy-blue palette</name>
  <files>src/components/main-panel.tsx</files>
  <read_first>src/components/main-panel.tsx (current implementation)</read_first>
  <action>
Rewrite `src/components/main-panel.tsx` to use the navy-blue palette. All child components (TerminalTabBar, AgentHeader, ServerPane) have already been updated in Plans 02-05. This plan updates the container/layout markup.

**Read first:** Read the current `src/components/main-panel.tsx`.

**Changes to apply:**
1. The terminal area `class="terminal-area flex-1 bg-bg-terminal..."` is already using `--color-bg-terminal` which we updated to #0B1120 in Plan 01. No change needed.
2. The p-3 pb-0 wrapper around AgentHeader: keep as-is (padding is fine).
3. The file viewer overlay:
   - outer: `style={{ backgroundColor: colors.bgBase }}` (replacing `bg-bg`)
   - header: `style={{ backgroundColor: colors.bgElevated, borderBottom: \`1px solid ${colors.bgBorder}\` }}` (replacing `bg-bg-raised border-b border-border`)
   - READ-ONLY badge: `style={{ backgroundColor: colors.accent, color: colors.bgBase }}` (replacing `bg-accent text-bg`)
   - Close button: update border to `border: \`1px solid ${colors.bgBorder}\``
   - pre block: update text color to `colors.textMuted`

4. The horizontal split handle (between main and server pane when expanded): update border color to `colors.bgBorder` if needed.

Keep the file viewer signals (fileViewerVisible, fileName, filePath, fileContent), keep the escape handler, keep all event listeners. Do NOT change the `terminal-containers` div structure.

**IMPORTANT:** Do not change the `div.terminal-containers.absolute.inset-0` structure. This is consumed by terminal-tabs.tsx for xterm.js mounting.
  </action>
  <verify>
    <automated>grep -n "terminal-containers\|terminal-area\|fileViewer" src/components/main-panel.tsx</automated>
  </verify>
  <done>main-panel.tsx uses navy-blue palette for file viewer overlay and split handle. terminal-containers div preserved for xterm.js mounting.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite right-panel.tsx with reference composition</name>
  <files>src/components/right-panel.tsx</files>
  <read_first>src/components/right-panel.tsx (current implementation)</read_first>
  <action>
Rewrite `src/components/right-panel.tsx` to use the navy-blue palette and reference composition pattern. The right panel is a container that routes between GSD/Diff/FileTree views and has a bash terminal at the bottom.

**Read first:** Read the current `src/components/right-panel.tsx` to understand the tab switching logic, GSD/Diff/FileTree routing, and bash terminal.

**Changes to apply:**
1. Add `import { colors, fonts } from '../tokens';`
2. Update the outer div:
   - `style={{ display: 'flex', flexDirection: 'column', height: '100%', backgroundColor: colors.bgBase, borderLeft: \`1px solid ${colors.bgBorder}\` }}`
   - Width is controlled by CSS class `.right-panel` + `--right-w` variable from drag-manager -- keep the class reference
3. The right panel tab bar: update background to `colors.bgBase` (bgBase = #111927), borderBottom to `colors.bgBorder`
4. Active tab button: `style={{ backgroundColor: colors.bgElevated, border: \`1px solid ${colors.bgSurface}\`, borderRadius: 6, padding: '6px 12px' }}`
5. Content areas (GSD, Diff, FileTree): already using bg-bg-terminal (bgDeep #0B1120) via their own components (gsd-viewer, diff-viewer, file-tree). They should be fine.
6. The horizontal split handle (between right-top and right-bottom): update to `colors.bgBorder`
7. Bash panel: update to use `colors.bgBase` for tab bar, `colors.bgDeep` for content area

Keep all existing tab switching signals, GSD/Diff/FileTree conditional rendering, bash terminal signals and lazy-connect logic.

The right-panel currently renders GsdViewer, DiffViewer, and FileTree as children. These have been updated in Plans 03-04. The panel itself just needs color updates.
  </action>
  <verify>
    <automated>grep -n "colors.bgBase\|colors.bgElevated\|colors.bgBorder\|colors.bgSurface" src/components/right-panel.tsx | head -10</automated>
  </verify>
  <done>right-panel.tsx uses navy-blue palette for tab bar and bash panel. All routing logic and bash terminal logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify container components</name>
  <files/>
  <action>N/A</action>
  <verify>N/A</verify>
  <done>N/A</done>
  <what-built>main-panel.tsx and right-panel.tsx with updated navy-blue palette</what-built>
  <how-to-verify>
1. Run `pnpm build` to verify compilation
2. Run `pnpm tauri dev` and visually inspect:
   - Main panel: terminal area is #0B1120 (bgDeep), AgentHeader shows gradient icon + status pill
   - File viewer overlay: READ-ONLY badge in accent blue, card uses bgElevated
   - Right panel: tab bar uses bgBase with pill-style active tab, content areas bgDeep
   - Bash terminal at bottom of right panel uses bgDeep content
3. Check that panel resizing still works (drag split handles)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Run `pnpm build` to verify compilation, then human verify in Tauri dev.
</verification>

<success_criteria>
- [ ] main-panel.tsx: terminal area bgDeep, file viewer uses navy-blue palette, terminal-containers preserved
- [ ] right-panel.tsx: bgBase tab bar, pill-style active tabs, bgDeep content areas, bash panel
- [ ] Panel resize still works (drag handles functional)
- [ ] All child components (AgentHeader, TerminalTabBar, ServerPane, GSDViewer, DiffViewer, FileTree) mount correctly
- [ ] pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-pixel-perfect-ui-rewrite/10-06-SUMMARY.md`
</output>