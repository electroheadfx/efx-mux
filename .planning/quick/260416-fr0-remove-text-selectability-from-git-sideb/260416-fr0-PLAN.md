---
phase: quick-260416-fr0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/git-control-tab.tsx]
autonomous: true
requirements: [UX-polish]
must_haves:
  truths:
    - "Clicking or dragging on accordion headers does not select text"
    - "Clicking or dragging on file rows does not select text"
    - "Clicking or dragging on history entries does not select text"
    - "Branch bar, toolbar, status bar text is not selectable"
    - "Commit textarea remains fully selectable and editable"
    - "Log panel entries remain selectable for copy-paste"
  artifacts:
    - path: "src/components/git-control-tab.tsx"
      provides: "userSelect none on all interactive elements"
      contains: "userSelect"
  key_links: []
---

<objective>
Add `userSelect: 'none'` to inline styles of all interactive/chrome elements in git-control-tab.tsx to prevent accidental text selection when clicking or dragging. Content areas (commit textarea, log entries) remain selectable.

Purpose: Eliminate UI noise from text selection on click/drag across sidebar interactive elements.
Output: Updated git-control-tab.tsx with non-selectable interactive chrome.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/git-control-tab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add userSelect none to interactive elements</name>
  <files>src/components/git-control-tab.tsx</files>
  <action>
Add `userSelect: 'none'` to the inline `style` object of each of these elements:

1. **CollapsibleSection header div** (the clickable div with `onClick={onToggle}`, ~line 338) -- add `userSelect: 'none'` to its style object.

2. **GitFileRow outer div** (~line 400, the div with `onClick` that opens diff) -- add `userSelect: 'none'` to its style object.

3. **HistoryEntry outer div** (~line 537, the div with onMouseEnter/Leave) -- add `userSelect: 'none'` to its style object.

4. **Header bar** (~line 732, the div with "changeSummary" and Stage All/Revert All buttons) -- add `userSelect: 'none'` to its style object.

5. **Branch bar** (~line 888, the div with GitBranch icon and branch name) -- add `userSelect: 'none'` to its style object.

6. **Bottom toolbar** (~line 996, the div with Pencil icon and Commit Tracked button) -- add `userSelect: 'none'` to its style object.

7. **Status bar** (~line 1056, the div with last commit message and action icons) -- add `userSelect: 'none'` to its style object.

8. **GitLogPanel header** (~line 609, the sticky div with "LOG (N)" and clear button) -- add `userSelect: 'none'` to its style object.

DO NOT add userSelect to:
- The commit `textarea` element (~line 957) -- must stay editable/selectable
- The log entry content divs (~line 658) -- users may want to copy error messages
- The scrollable file list container (~line 806) -- just a wrapper, children handle it
- The empty state div (~line 814) -- harmless text, not interactive
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && grep -c "userSelect" src/components/git-control-tab.tsx</automated>
  </verify>
  <done>All 8 interactive element groups have userSelect: 'none' in their inline styles. Commit textarea and log entry content remain selectable. Grep count confirms at least 8 occurrences of userSelect in the file.</done>
</task>

</tasks>

<verification>
- `grep -n "userSelect" src/components/git-control-tab.tsx` shows 8+ lines with userSelect: 'none'
- None of those lines are inside the textarea or log entry content divs
- `pnpm tsc --noEmit` passes (no type errors)
</verification>

<success_criteria>
All interactive chrome elements in git-control-tab.tsx are non-selectable. Commit textarea and log panel content remain selectable. No TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-fr0-remove-text-selectability-from-git-sideb/260416-fr0-SUMMARY.md`
</output>
