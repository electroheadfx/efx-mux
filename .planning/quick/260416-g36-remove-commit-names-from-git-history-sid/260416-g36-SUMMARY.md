---
status: complete
phase: quick
plan: 260416-g36
subsystem: git-sidebar
tags: [ui, git, sidebar, tooltip]
dependency_graph:
  requires: []
  provides: [git-history-tooltip]
  affects: [git-control-tab]
tech_stack:
  added: []
  patterns: [html-title-tooltip]
key_files:
  created: []
  modified:
    - src/components/git-control-tab.tsx
decisions:
  - Used native HTML title attribute for tooltip (lightweight, no dependency)
metrics:
  duration: 41s
  completed: "2026-04-16T09:37:32Z"
---

# Quick Task 260416-g36: Remove Commit Names from Git History Sidebar

Moved author name from inline display in git history rows to a native hover tooltip showing author, short hash, and full timestamp.

## Changes Made

### Task 1: Move author name from visible row to tooltip in HistoryEntry

**Commit:** `2434cd0`

Two edits to the `HistoryEntry` component in `git-control-tab.tsx`:

1. Added `title` attribute to the outer div: `"{author} -- {short_hash} -- {full datetime}"`
2. Removed `{entry.author}, ` from the right-aligned span, leaving only `{relativeTime(entry.timestamp)}`

**Result:** History rows now show only commit message, ref badges, and relative time. Hovering reveals full author, hash, and timestamp in a native tooltip.

## Deviations from Plan

None -- plan executed exactly as written.

## Verification

- TypeScript compiles with zero errors (`pnpm exec tsc --noEmit`)
- History rows display commit message + relative time only (no author name)
- Hover tooltip shows: `{author} -- {short_hash} -- {full date/time}`

## Self-Check: PASSED
