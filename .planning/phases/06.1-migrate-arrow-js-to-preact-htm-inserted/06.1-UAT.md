---
status: diagnosed
phase: 06.1-migrate-arrow-js-to-preact-htm-inserted
source: [06.1-01-SUMMARY.md, 06.1-02-SUMMARY.md, 06.1-03-SUMMARY.md, 06.1-04-SUMMARY.md, 06.1-05-SUMMARY.md]
started: 2026-04-08T15:00:00Z
updated: 2026-04-08T15:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running dev server. Run `pnpm install` then `pnpm run tauri dev`. Server boots without errors, Vite dev server starts on port 1420, and the Tauri window opens showing the application.
result: pass

### 2. Three-Panel Layout
expected: The app window shows three panels: a left sidebar with project list, a center main terminal area, and a right panel with tabbed views. All three are visible and properly sized.
result: issue
reported: "yes but with a strange proportion, I think there is small layout issue with tailwind"
severity: major

### 3. Solarized Dark Theme
expected: The app uses Solarized Dark colors — dark background (#282d3a), blue accent (#258ad1) on active elements. Text is readable, no unstyled/white elements.
result: pass

### 4. Terminal Connection
expected: The main terminal panel connects to a PTY shell session. You can type commands and see output. The terminal cursor blinks and responds to input.
result: pass

### 5. Panel Drag Resize
expected: Dragging the vertical handle between sidebar and main panel resizes them. Dragging the vertical handle between main panel and right panel resizes them. Panels respect minimum widths.
result: issue
reported: "NO I can't there is a layout issue with error log: 'ResizeObserver loop completed with undelivered notifications.'"
severity: blocker

### 6. Sidebar Projects
expected: The sidebar shows a list of registered projects. Clicking a project name switches the active project. Git status (branch, changed files count) displays for the active project.
result: pass

### 7. Add Project Modal
expected: Clicking the "+" button in the sidebar opens the Add Project modal. The modal has a directory browser, project name field, and form validation. Submitting adds the project to the sidebar list.
result: issue
reported: "yes and with an awfull dialog modal window, it need some tailwind tweaks (padding, margin, border)"
severity: cosmetic

### 8. Fuzzy Project Search
expected: Pressing Ctrl+P opens a fuzzy search overlay. Typing filters the project list. Selecting a result switches to that project. Pressing Escape closes the overlay.
result: pass

### 9. Right Panel Tabs
expected: The right panel top section has tabs for GSD, Diff, and File Tree. Clicking each tab switches the view. GSD tab shows markdown content. File Tree tab shows directory listing.
result: issue
reported: "yes but with an awfull UI/UX design all the border, padding, margin have left, plus the Pane layout issue"
severity: major

### 10. Production Build
expected: Running `pnpm run build` completes without errors and produces output in the dist/ directory.
result: pass

## Summary

total: 10
passed: 5
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Three panels visible and properly sized"
  status: failed
  reason: "User reported: yes but with a strange proportion, I think there is small layout issue with tailwind"
  severity: major
  test: 2
  root_cause: "Layout CSS rules for .sidebar, .main-panel, .right-panel were in old layout.css which was deleted in Plan 05 cleanup but never ported to app.css. The CSS custom properties --sidebar-w and --right-w are set by drag-manager but no CSS rules consume them."
  artifacts:
    - path: "src/styles/app.css"
      issue: "Missing .sidebar { width: var(--sidebar-w) }, .main-panel { flex: 1 }, .right-panel { width: var(--right-w) } rules"
  missing:
    - "Add structural layout CSS rules for .sidebar, .main-panel, .right-panel, .right-top, .right-bottom, .server-pane to app.css"
  debug_session: ""

- truth: "Drag handles resize panels, panels respect minimum widths"
  status: failed
  reason: "User reported: NO I can't there is a layout issue with error log: 'ResizeObserver loop completed with undelivered notifications.'"
  severity: blocker
  test: 5
  root_cause: "Two issues: (1) Missing layout CSS means drag CSS var writes have no visual effect. (2) fitAddon.fit() called synchronously inside ResizeObserver callback in resize-handler.ts causes infinite observation loop — needs requestAnimationFrame wrapper."
  artifacts:
    - path: "src/terminal/resize-handler.ts"
      issue: "fitAddon.fit() at line ~26 called synchronously inside ResizeObserver callback triggers layout changes that re-fire the observer"
    - path: "src/styles/app.css"
      issue: "Missing layout rules — same as test 2"
  missing:
    - "Wrap fitAddon.fit() in requestAnimationFrame() in resize-handler.ts"
    - "Add missing layout CSS rules (same fix as test 2)"
  debug_session: ""

- truth: "Add Project modal has proper styling with form layout"
  status: failed
  reason: "User reported: yes and with an awfull dialog modal window, it need some tailwind tweaks (padding, margin, border)"
  severity: cosmetic
  test: 7
  root_cause: "Modal Tailwind classes are present but may not render correctly due to possible CSS double-load (index.html <link> + main.tsx import). Additionally, form spacing and borders need visual refinement — padding/gap values too tight."
  artifacts:
    - path: "src/components/project-modal.tsx"
      issue: "Form field spacing and border styling need Tailwind refinement"
    - path: "index.html"
      issue: "Possible CSS double-load — <link> tag loads unprocessed app.css alongside Vite-processed import"
  missing:
    - "Remove <link rel='stylesheet'> from index.html (Vite import in main.tsx is the correct loader)"
    - "Refine modal form padding, gap, and border styling"
  debug_session: ""

- truth: "Right panel tabs switch views with proper styling"
  status: failed
  reason: "User reported: yes but with an awfull UI/UX design all the border, padding, margin have left, plus the Pane layout issue"
  severity: major
  test: 9
  root_cause: "Same root cause as test 2 — .right-panel, .right-top, .right-bottom have no CSS layout rules. Without flex context from parent, child Tailwind flex classes collapse to zero height. Tab bar CSS (.tab-bar, .tab-btn) also missing from app.css."
  artifacts:
    - path: "src/styles/app.css"
      issue: "Missing .right-top, .right-bottom flex layout rules and .tab-bar/.tab-btn styling"
  missing:
    - "Add .right-top, .right-bottom, .tab-bar, .tab-btn CSS rules to app.css (same fix as test 2)"
  debug_session: ""
