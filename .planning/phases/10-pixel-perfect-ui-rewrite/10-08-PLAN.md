---
phase: 10
plan: '08'
type: execute
wave: 2
depends_on: ['10-01']
files_modified:
  - src/components/project-modal.tsx
  - src/components/preferences-panel.tsx
autonomous: true
requirements:
  - UI-01
  - UI-04
  - PROJ-01

must_haves:
  truths:
    - "project-modal.tsx uses reference AddProjectModal visual (bgElevated card, bgDeep inputs, accentMuted browse button, 12px radius)"
    - "preferences-panel.tsx uses same modal treatment as AddProjectModal"
    - "All form validation, browse, add/edit logic preserved"
  artifacts:
    - path: "src/components/project-modal.tsx"
      provides: "Add/Edit Project modal with form validation, browse, navy-blue palette"
      contains: "colors.bgElevated"
    - path: "src/components/preferences-panel.tsx"
      provides: "Preferences panel with settings, theme toggle, project editing"
      contains: "bg-bg-raised"
---

<objective>
Rewrite project-modal.tsx and preferences-panel.tsx with navy-blue palette and reference modal patterns. Both modals use the same visual structure (overlay, card, header/divider/form/footer) and should match the reference AddProjectModal.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/project-modal.tsx (current — keep all logic)
@src/components/preferences-panel.tsx (current — keep all logic)
@RESEARCH/theme/AddProjectModal.tsx (reference visual pattern)
@RESEARCH/theme/PreferencesPanel.tsx (reference visual pattern)

# Reference modal patterns
## AddProjectModal
- Overlay: bg-black/50 equivalent (`backgroundColor: 'rgba(0,0,0,0.5)'`)
- Card: bgElevated (#19243A), border bgSurface (#324568), 12px radius, shadow-2xl
- Width: 520px
- Header: 20px 24px, font-semibold 16px textPrimary
- Divider: 1px bgBorder
- Form: 20px 24px, gap-16
- Input: bgBase (#111927), border bgSurface, 8px radius, 10px 12px padding, textPrimary/textMuted
- Browse button: bgBorder background, accent text
- Footer: 16px 24px, Discard (ghost border bgSurface) + Add Project (accent fill)
- Agent icon: gradient agentGradientStart→End, 20x20, diamond

## PreferencesPanel
- Same overlay/card treatment
- Section labels: GeistMono uppercase, textDim
- Setting rows: flex with label (textMuted) left, control right
- Theme toggle: accent background when dark
- Keycap badges: bgDeep fill, bgBorder border, 4px radius
- Agent badge: gradient background
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite project-modal.tsx with reference modal styling</name>
  <files>src/components/project-modal.tsx</files>
  <read_first>src/components/project-modal.tsx (full implementation — keep all signals, form logic, event listeners)</read_first>
  <action>
Rewrite `src/components/project-modal.tsx` visual styles to match the reference AddProjectModal while preserving all existing logic (signals, form validation, browse, add/edit modes).

**Read first:** Read the ENTIRE current `src/components/project-modal.tsx` to understand the component structure (visible signal, form fields, handleSubmit, handleBrowse, first-run mode).

**Changes to apply:**

1. **Add tokens import:**
   `import { colors, fonts, fontSizes, spacing, radii } from '../tokens';`

2. **Update overlay (line 149):**
   - Replace `class="fixed inset-0 bg-black/50 z-[100] flex items-center justify-center animate-[fadeIn_150ms_ease-out]"`
   - With `style={{ position: 'fixed', inset: 0, backgroundColor: 'rgba(0,0,0,0.5)', zIndex: 100, display: 'flex', alignItems: 'center', justifyContent: 'center' }}`

3. **Update modal card (line 155):**
   - Replace `class="w-[480px] bg-bg-raised border border-border-interactive rounded-xl shadow-2xl z-[101] animate-[fadeIn_150ms_ease-out]"`
   - With `style={{ width: 520, backgroundColor: colors.bgElevated, border: \`1px solid ${colors.bgSurface}\`, borderRadius: 12, boxShadow: '0 8px 32px rgba(0,0,0,0.6)', zIndex: 101 }}`
   - Note: 520px width per reference (not 480px)

4. **Update header (line 159):**
   - `class="flex items-center px-6 pt-5 pb-4 justify-between"`
   - With `style={{ display: 'flex', alignItems: 'center', padding: '20px 24px 16px', justifyContent: 'space-between' }}`
   - Title: `style={{ fontFamily: fonts.sans, fontSize: 16, fontWeight: 600, color: colors.textPrimary }}`
   - Close button: update border to `\`1px solid ${colors.bgSurface}\``

5. **Update divider (line 169):**
   - `class="h-px bg-border w-full"`
   - With `style={{ height: 1, backgroundColor: colors.bgBorder }}`

6. **Update form (line 172-249):**
   - Form container: `style={{ padding: '20px 24px', gap: 16, display: 'flex', flexDirection: 'column' }}`
   - Field labels: use `style={{ fontFamily: fonts.mono, fontSize: fontSizes.sm, color: colors.textDim, letterSpacing: '1.2px', marginBottom: 6 }}`
   - Input fields: update to use `style={{ backgroundColor: colors.bgDeep, border: \`1px solid ${colors.bgSurface}\`, borderRadius: 8, padding: '10px 12px', fontSize: 13, color: colors.textPrimary, fontFamily: fonts.sans }}`
   - Input placeholder: use `style={{ color: colors.textDim }}` (placeholder text will use browser default)
   - Browse button: update to `style={{ backgroundColor: colors.bgBorder, border: \`1px solid ${colors.bgSurface}\`, borderRadius: 8, padding: '8px 12px', color: colors.textMuted, fontFamily: fonts.mono, fontSize: 11 }}`

7. **Update footer buttons (line 259-271):**
   - Cancel button: `style={{ borderRadius: 8, border: \`1px solid ${colors.bgSurface}\`, padding: '8px 16px', fontSize: 13, fontFamily: fonts.sans, color: colors.textMuted }}`
   - Submit button: `style={{ borderRadius: 8, backgroundColor: colors.accent, border: 'none', padding: '8px 20px', fontSize: 13, fontWeight: 600, fontFamily: fonts.sans, color: 'white' }}`

**IMPORTANT:** Keep all existing signals (visible, directory, name, agent, gsdFile, serverCmd, error, isFirstRun, editingName), keep `isValid` computed, keep `openProjectModal`/`closeProjectModal` functions, keep `handleSubmit`/`handleBrowse` implementations, keep all event listeners. Only update visual styles.

The modal width is increased from 480px to 520px per reference. Keep the form fields (Directory, Name, Agent, GSD File, Server Command) as-is, only update their visual styling.
  </action>
  <verify>
    <automated>grep -n "colors.bgElevated\|colors.bgSurface\|colors.bgDeep\|colors.accent" src/components/project-modal.tsx | head -10</automated>
  </verify>
  <done>project-modal.tsx uses reference modal styling (bgElevated card, bgDeep inputs, bgSurface borders, 12px radius, 520px width). All form logic preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite preferences-panel.tsx with reference modal styling</name>
  <files>src/components/preferences-panel.tsx</files>
  <read_first>src/components/preferences-panel.tsx (current — understand settings, theme toggle, project edit)</read_first>
  <action>
Rewrite `src/components/preferences-panel.tsx` visual styles to match the reference PreferencesPanel pattern. The component has a similar modal structure to project-modal.

**Read first:** Read the current `src/components/preferences-panel.tsx` to understand the structure (visible signal, settings display, theme toggle, close handlers).

**Changes to apply:**

1. **Add tokens import:**
   `import { colors, fonts } from '../tokens';`

2. **Update overlay (line 53):**
   - Replace `class="fixed inset-0 bg-black/30 z-[100] flex items-center justify-center"`
   - With `style={{ position: 'fixed', inset: 0, backgroundColor: 'rgba(0,0,0,0.5)', zIndex: 100, display: 'flex', alignItems: 'center', justifyContent: 'center' }}`

3. **Update modal card (line 57):**
   - Replace `class="w-[500px] max-h-[70vh] bg-bg-raised border border-border-interactive rounded-xl shadow-2xl overflow-y-auto"`
   - With `style={{ width: 500, maxHeight: '70vh', backgroundColor: colors.bgElevated, border: \`1px solid ${colors.bgBorder}\`, borderRadius: 12, overflowY: 'auto', boxShadow: '0 8px 32px rgba(0,0,0,0.6)' }}`

4. **Update header:**
   - border-bottom to use `colors.bgBorder`

5. **Update section labels:**
   - Use `style={{ fontFamily: fonts.mono, fontSize: 10, color: colors.textDim, textTransform: 'uppercase', letterSpacing: '1.5px' }}`

6. **Update setting rows:**
   - Update text colors to use `colors.textMuted` for labels, `colors.textPrimary` for values

Keep all existing functionality (visible signal, togglePreferences/closePreferences, theme toggle, project editing integration).
  </action>
  <verify>
    <automated>grep -n "colors.bgElevated\|colors.bgBorder\|colors.textMuted" src/components/preferences-panel.tsx | head -10</automated>
  </verify>
  <done>preferences-panel.tsx uses navy-blue modal styling (bgElevated, bgBorder, textMuted, textPrimary). All settings display and theme toggle logic preserved.</done>
</task>

</tasks>

<verification>
Run `pnpm build` to verify both modals compile correctly.
</verification>

<success_criteria>
- [ ] project-modal.tsx: 520px width, bgElevated card, bgSurface borders, 12px radius, all form fields styled
- [ ] preferences-panel.tsx: bgElevated card, section labels in uppercase mono, setting rows styled
- [ ] Both modals use same overlay background (rgba(0,0,0,0.5))
- [ ] All form logic preserved (project-modal: signals, validation, browse; preferences-panel: visible, toggle, settings)
- [ ] pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-pixel-perfect-ui-rewrite/10-08-SUMMARY.md`
</output>