# Phase 6.1: Migrate Arrow.js to Preact + htm (INSERTED) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-08
**Phase:** 06.1-migrate-arrow-js-to-preact-htm-inserted
**Areas discussed:** Migration strategy, State management, Component API pattern, Vendor setup + import map, CSS approach

---

## Migration Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Big-bang rewrite | Replace all Arrow.js usage in one phase. Clean cut. | :heavy_check_mark: |
| Incremental (component-by-component) | Migrate one component at a time. Requires dual reactive systems. | |
| Strangler fig (new code only) | Only new components use Preact. Longest timeline. | |

**User's choice:** Big-bang rewrite
**Notes:** User asked "Not better to manage in ESM with TypeScript and Vite? or in js/cjs? I want too tailwind 4." — expanded scope from framework swap to full toolchain modernization.

---

## Scope Expansion

| Option | Description | Selected |
|--------|-------------|----------|
| Full modernization in 6.1 | Vite + Preact + TypeScript + Tailwind 4 in one phase | :heavy_check_mark: |
| Framework swap only in 6.1 | Keep no-bundler ESM, add toolchain as Phase 6.2 | |
| Vite + Preact + Tailwind (skip TS) | Add build step and Tailwind but keep plain JS | |

**User's choice:** Full modernization in 6.1
**Notes:** Since every component gets rewritten anyway, adding the toolchain now avoids touching them again later.

---

## State Management

| Option | Description | Selected |
|--------|-------------|----------|
| @preact/signals | Fine-grained reactivity. signal(), computed(), effect(). Closest to Arrow.js reactive(). | :heavy_check_mark: |
| useState + useContext | Standard React/Preact hooks. More boilerplate. | |
| Zustand | Lightweight external store. Single store with selectors. | |

**User's choice:** @preact/signals
**Notes:** None

---

## Component API Pattern (Templates)

| Option | Description | Selected |
|--------|-------------|----------|
| JSX/TSX | Standard Preact JSX with .tsx files. Full TS type checking in templates. | :heavy_check_mark: |
| htm tagged templates | Keep tagged template literal style. No JSX transform needed. | |
| Mix: JSX + htm | Main components in .tsx, htm for small inline templates. | |

**User's choice:** JSX/TSX
**Notes:** None

---

## Build Setup

| Option | Description | Selected |
|--------|-------------|----------|
| Vite dev + Tauri dev | npm deps, Vite dev server for HMR, Vite builds to dist/. Remove vendor/ and importmap. | :heavy_check_mark: |
| Vite build only | Use Vite just as bundler, no dev server. | |
| Keep vendor + add Vite on top | Keep vendored ESM as fallback. Most conservative. | |

**User's choice:** Vite dev + Tauri dev
**Notes:** None

---

## CSS / Tailwind Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Convert to Tailwind utilities | Rewrite theme.css and layout.css as Tailwind classes. Solarized Dark as theme tokens. | :heavy_check_mark: |
| Hybrid: Tailwind + custom CSS | Tailwind for new styling, keep existing CSS files. | |
| Tailwind for components, CSS for layout | Layout stays in CSS, components use Tailwind. | |

**User's choice:** Convert to Tailwind utilities
**Notes:** None

---

## Claude's Discretion

- Vite config details (aliases, plugin order)
- TypeScript strictness level
- File/folder reorganization within src/
- Tailwind utility class choices
- Whether to convert drag-manager.js to TypeScript

## Deferred Ideas

None
