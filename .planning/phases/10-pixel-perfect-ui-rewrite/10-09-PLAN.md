---
phase: 10
plan: '09'
type: execute
wave: 9
depends_on: ['10-01']
files_modified:
  - src/components/terminal-tabs.tsx
  - src/components/first-run-wizard.tsx
  - src/components/fuzzy-search.tsx
  - src/components/shortcut-cheatsheet.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UX-01

must_haves:
  truths:
    - "terminal-tabs.tsx TerminalTabBar uses bottom-border 2px accent for active tab, textDim for inactive"
    - "first-run-wizard.tsx, fuzzy-search.tsx, shortcut-cheatsheet.tsx use navy-blue palette for overlay and card styles"
  artifacts:
    - path: "src/components/terminal-tabs.tsx"
      provides: "Terminal tab bar with accent active indicator, bgBase container"
      contains: "border-b-2 border-accent"
    - path: "src/components/shortcut-cheatsheet.tsx"
      provides: "Shortcut cheatsheet overlay with navy-blue card styling"
      contains: "bg-bg-raised"
---

<objective>
Update remaining components that need navy-blue palette updates: TerminalTabBar in terminal-tabs.tsx (tab bar styling), and three overlay/styling components (first-run-wizard, fuzzy-search, shortcut-cheatsheet) that need visual restyling.

The TerminalTabBar is the most critical -- it's the main panel tab bar with bottom-border active indicator. The three overlays are styling-only updates per "Claude's discretion" in D-14.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/terminal-tabs.tsx (TerminalTabBar section, lines ~528-571)
@src/components/first-run-wizard.tsx (current -- multi-step wizard modal)
@src/components/fuzzy-search.tsx (current -- project search overlay)
@src/components/shortcut-cheatsheet.tsx (current -- keyboard shortcut display)

# Reference patterns

## TerminalTabBar (main panel tab bar)
- Container: bgBase (#111927), borderBottom bgBorder
- Active tab: border-bottom 2px accent (#258AD1), textPrimary (#E6EDF3)
- Inactive tab: textDim (#556A85)
- Active dot: 6px circle, statusGreen (#3FB950)
- Close button: textDim, hover textPrimary

## Overlay patterns (first-run, fuzzy-search, cheatsheet)
- Overlay: bg-black/50 equivalent
- Card: bgElevated (#19243A), border bgBorder, rounded-12
- Content: use navy-blue text colors

# TerminalTabBar current styling (lines 533-569)
Active: `border-b-2 border-accent text-text-bright bg-transparent`
Inactive: `text-text-muted font-sans cursor-pointer bg-transparent`
New tab: `text-text-muted hover:bg-bg-raised`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TerminalTabBar styling</name>
  <files>src/components/terminal-tabs.tsx</files>
  <read_first>src/components/terminal-tabs.tsx (lines 528-571, the TerminalTabBar component)</read_first>
  <action>
Update `TerminalTabBar` component in `src/components/terminal-tabs.tsx` to explicitly use navy-blue palette via inline styles where needed. The active tab indicator uses `border-b-2 border-accent` which is correct (#258AD1). The key is ensuring inactive tabs use `text-text-muted` (which maps to --color-text-muted = #556A85 in the new palette) and active tabs use `text-text-bright` (#E6EDF3).

**Read first:** Read lines 528-571 of `src/components/terminal-tabs.tsx` to see the current TerminalTabBar implementation.

**Changes to apply:**
1. Add `import { colors, fonts } from '../tokens';` near the top of the file (after existing imports)
2. Update the container div (line 533):
   - Current: `class="flex gap-0.5 px-2 bg-bg border-b border-border shrink-0 items-center h-[34px]"`
   - The Tailwind classes are mostly fine (bg-bg maps to #111927 via @theme). Keep structural classes, add explicit color via style if needed.
   - Add `style={{ backgroundColor: colors.bgBase, borderBottom: \`1px solid ${colors.bgBorder}\` }}` to ensure correct colors
3. Update active tab button:
   - Keep `border-b-2 border-accent` (accent #258AD1 is correct)
   - Update text to use `style={{ color: colors.textPrimary }}` inline
   - The active dot `w-1.5 h-1.5 rounded-full bg-success` should use `style={{ width: 6, height: 6, borderRadius: '50%', backgroundColor: colors.statusGreen }}`
4. Update inactive tab:
   - Replace `text-text-muted` with inline style `style={{ color: colors.textDim }}`
5. Update close button span:
   - Replace `text-text-muted` with `style={{ color: colors.textDim }}`
6. Update new tab button:
   - Keep `text-text-muted hover:bg-bg-raised` but add inline style for the hover background to ensure bgElevated (#19243A)

The Tailwind classes for structural layout (flex, gap, px, py, etc.) should be kept. Only replace color-related Tailwind classes with inline styles using tokens.ts values.

**IMPORTANT:** Keep all terminal management logic (createNewTab, closeTab, switchToTab, cycleToNextTab, getActiveTerminal, initFirstTab, etc.) exactly as-is. Only update the TerminalTabBar visual rendering.
  </action>
  <verify>
    <automated>grep -n "colors.statusGreen\|colors.bgBase\|colors.bgBorder\|colors.textDim" src/components/terminal-tabs.tsx | head -10</automated>
  </verify>
  <done>TerminalTabBar uses navy-blue palette for tab styling (bgBase container, bgBorder divider, statusGreen active dot, textDim inactive text). All terminal management logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update first-run-wizard.tsx styling</name>
  <files>src/components/first-run-wizard.tsx</files>
  <read_first>src/components/first-run-wizard.tsx (current -- understand multi-step wizard structure)</read_first>
  <action>
Update `src/components/first-run-wizard.tsx` styling to use the navy-blue palette for the wizard modal. This is a multi-step wizard modal that needs the overlay and card styling updated.

**Read first:** Read the current `src/components/first-run-wizard.tsx` to understand the structure (steps, form fields, buttons).

**Changes to apply (key surfaces only):**
1. Add `import { colors, fonts } from '../tokens';`
2. Update the modal overlay: `bg-black/50` → `style={{ backgroundColor: 'rgba(0,0,0,0.5)' }}`
3. Update the modal card background: `bg-bg-raised` → `style={{ backgroundColor: colors.bgElevated }}`
4. Update borders: `border-border` → `colors.bgBorder`
5. Update text colors: `text-text-bright` → `colors.textPrimary`, `text-text` → `colors.textMuted`
6. Update step indicators if they use status colors (green for completed, etc.)

Focus on the outer modal card styling (background, border, shadow). Internal step content styling can remain mostly the same as it's already using dark palette values that are compatible.
  </action>
  <verify>
    <automated>grep -n "bg-bg-raised\|border-border\|text-text-bright" src/components/first-run-wizard.tsx | head -5</automated>
  </verify>
  <done>first-run-wizard.tsx uses navy-blue palette for modal overlay and card. All wizard steps and form logic preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update fuzzy-search.tsx styling</name>
  <files>src/components/fuzzy-search.tsx</files>
  <read_first>src/components/fuzzy-search.tsx (current -- understand project search overlay)</read_first>
  <action>
Update `src/components/fuzzy-search.tsx` styling to use the navy-blue palette for the fuzzy search overlay/modal.

**Read first:** Read the current `src/components/fuzzy-search.tsx` to understand the structure (input field, results list, project items).

**Changes to apply:**
1. Add `import { colors, fonts } from '../tokens';`
2. Update the overlay background if it uses dark overlay
3. Update the input field to use bgDeep (#0B1120) with bgBorder border
4. Update result items: active item uses bgElevated, inactive uses transparent with textMuted

Focus on the input field styling (bgDeep background, accent focus border) and the result list item styling.
  </action>
  <verify>
    <automated>grep -n "bg-bg\|border-border\|text-text" src/components/fuzzy-search.tsx | head -5</automated>
  </verify>
  <done>fuzzy-search.tsx uses navy-blue palette for overlay and input field. All search logic preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Update shortcut-cheatsheet.tsx styling</name>
  <files>src/components/shortcut-cheatsheet.tsx</files>
  <read_first>src/components/shortcut-cheatsheet.tsx (current -- understand shortcut display)</read_first>
  <action>
Update `src/components/shortcut-cheatsheet.tsx` styling to use the navy-blue palette for the cheatsheet overlay.

**Read first:** Read the current `src/components/shortcut-cheatsheet.tsx` to understand the structure (overlay, card, shortcut rows).

**Changes to apply:**
1. Add `import { colors, fonts } from '../tokens';`
2. Update the overlay background: `bg-black/60` → `style={{ backgroundColor: 'rgba(0,0,0,0.6)' }}`
3. Update the card: `bg-bg-raised` → `style={{ backgroundColor: colors.bgElevated, border: \`1px solid ${colors.bgBorder}\` }}`
4. Update shortcut rows: key text, action text using appropriate colors from tokens.ts
5. Keycap badges: use `style={{ backgroundColor: colors.bgDeep, border: \`1px solid ${colors.bgBorder}\`, borderRadius: 4, padding: '2px 6px', fontFamily: fonts.mono, fontSize: 10 }}`

This is a static display component so no logic changes needed -- only visual styles.
  </action>
  <verify>
    <automated>grep -n "bg-bg-raised\|border-border" src/components/shortcut-cheatsheet.tsx | head -5</automated>
  </verify>
  <done>shortcut-cheatsheet.tsx uses navy-blue palette for overlay and card. All shortcut display logic preserved.</done>
</task>

</tasks>

<verification>
Run `pnpm build` to verify all updated components compile correctly.
</verification>

<success_criteria>
- [ ] TerminalTabBar: active tab has bottom-border 2px accent, statusGreen dot, textPrimary; inactive has textDim
- [ ] first-run-wizard.tsx: modal uses bgElevated card, bgBorder border, all wizard logic preserved
- [ ] fuzzy-search.tsx: input uses bgDeep, accent focus border, search logic preserved
- [ ] shortcut-cheatsheet.tsx: card uses bgElevated, keycaps use bgDeep + bgBorder border
- [ ] pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-pixel-perfect-ui-rewrite/10-09-SUMMARY.md`
</output>