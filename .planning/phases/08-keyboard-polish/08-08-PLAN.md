---
phase: 08-keyboard-polish
plan: 08
type: execute
wave: 4
depends_on: ['06', '07']
files_modified:
  - src/components/preferences-panel.tsx
  - src/main.tsx
  - src/components/shortcut-cheatsheet.tsx
autonomous: true
gap_closure: true
requirements: [UX-01]

must_haves:
  truths:
    - "Ctrl+, opens a preferences panel overlay"
    - "Preferences panel shows current project settings (agent, server command, theme)"
    - "Preferences panel dismisses with Escape or click outside"
    - "Ctrl+, appears in the shortcut cheatsheet"
  artifacts:
    - path: "src/components/preferences-panel.tsx"
      provides: "Preferences panel component"
    - path: "src/main.tsx"
      provides: "Ctrl+, keyboard handler + PreferencesPanel mount"
    - path: "src/components/shortcut-cheatsheet.tsx"
      provides: "Ctrl+, entry in cheatsheet"
  key_links:
    - from: "keydown handler"
      to: "togglePreferences"
      via: "Ctrl+, detection"
      pattern: "comma.*preferences|togglePreferences"
---

<objective>
Implement Ctrl+, preferences panel (UAT test 15). The feature is entirely missing -- no keyboard handler for comma, no component.

Purpose: Complete the last missing keyboard shortcut from the phase spec. Plan 07 fixes the main.tsx keyboard guard (allowing metaKey through), and Plan 06 modifies main.tsx for tab restore -- both must complete first.
Output: Working preferences overlay accessible via Ctrl+, showing project settings.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-keyboard-polish/08-01-SUMMARY.md
@.planning/phases/08-keyboard-polish/08-07-SUMMARY.md
@.planning/phases/08-keyboard-polish/08-06-SUMMARY.md
@src/main.tsx
@src/components/shortcut-cheatsheet.tsx
@src/components/first-run-wizard.tsx
@src/state-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create preferences panel component</name>
  <files>src/components/preferences-panel.tsx</files>
  <action>
Create a new component `preferences-panel.tsx` following the same overlay pattern as `shortcut-cheatsheet.tsx` and `fuzzy-search.tsx`.

Structure:
1. Module-level signal: `const visible = signal(false);`
2. Export `togglePreferences()` and `closePreferences()` functions.
3. Component `PreferencesPanel` renders when visible:
   - Fixed overlay backdrop (bg-black/30, z-[100]) -- click to dismiss.
   - Centered panel (w-[500px], max-h-[70vh], bg-bg-raised, border, rounded-lg, shadow).
   - Header: "Preferences" title with X close button.
   - Sections showing current project settings (read-only display, not editable -- editing is done via project modal):

   **Current Project section:**
   - Project name: from `activeProjectName` signal
   - Project path: from `projects` signal (find active project)
   - Agent: from active project's `agent` field
   - Display each as a labeled row (label on left, value on right, same style as cheatsheet rows).

   **Appearance section:**
   - Current theme mode: "Dark" or "Light" (read from document class or theme state)
   - Toggle dark/light button using `toggleThemeMode` from theme-manager.

   **Shortcuts section:**
   - Brief text: "Press Ctrl+? to view all keyboard shortcuts"

   **Actions section:**
   - "Edit Project" button that opens the project modal (import `openProjectModal` from project-modal).

4. Escape key dismissal: useEffect that listens for Escape when visible, calls closePreferences().
5. Import signals from state-manager: `activeProjectName`, `projects`.
6. Import `toggleThemeMode` from theme-manager.
7. Import `openProjectModal` from project-modal.

Style: Use same Tailwind classes as shortcut-cheatsheet for consistency (bg-bg-raised, border-border, text-text-bright, etc).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - preferences-panel.tsx exists with PreferencesPanel component
    - togglePreferences and closePreferences exported
    - Shows current project name, path, agent, theme mode
    - Dismisses on Escape and click outside
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Ctrl+, handler + mount component + update cheatsheet</name>
  <files>src/main.tsx, src/components/shortcut-cheatsheet.tsx</files>
  <action>
**In src/main.tsx:**

1. Import: `import { PreferencesPanel, togglePreferences } from './components/preferences-panel';`

2. Add `<PreferencesPanel />` to the App component JSX, alongside other overlays (after `<FirstRunWizard />`).

3. In the keyboard handler switch block, add a case for comma.
   Note: `e.key` for the comma key is `','`. Add the case:
   ```typescript
   case key === ',' && e.ctrlKey && !e.shiftKey && !e.altKey:
     e.preventDefault(); e.stopPropagation();
     togglePreferences();
     break;
   ```
   Also handle Cmd+, (macOS convention):
   ```typescript
   case key === ',' && e.metaKey && !e.shiftKey && !e.altKey:
     e.preventDefault(); e.stopPropagation();
     togglePreferences();
     break;
   ```
   Or combine into one case:
   ```typescript
   case key === ',' && (e.ctrlKey || e.metaKey) && !e.shiftKey && !e.altKey:
   ```

   IMPORTANT: The keyboard guard was fixed in Plan 07 to allow both ctrlKey and metaKey through (`if (!e.ctrlKey && !e.metaKey) return`). The Cmd+, case will work correctly with that guard in place. Do NOT revert the guard change.

   IMPORTANT: `e.key` for comma is literally `','`. But `key` is set via `e.key.toLowerCase()` -- comma has no case, so it stays `','`. Test that `key === ','` works correctly.

**In src/components/shortcut-cheatsheet.tsx:**

Add Ctrl+, to the SHORTCUTS array. In the 'App' section, add:
```typescript
{ key: 'Ctrl+,', action: 'Preferences' },
```
Add it before the cheatsheet entry so preferences appears before "This cheatsheet".
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Ctrl+, opens preferences panel
    - Cmd+, also opens preferences panel (macOS convention)
    - PreferencesPanel mounted in App component
    - Cheatsheet includes Ctrl+, entry
    - TypeScript compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Preferences panel is read-only display of local state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | I | preferences display | accept | Read-only display of project config already in memory; no sensitive data exposed |
</threat_model>

<verification>
1. `pnpm exec tsc --noEmit` passes
2. Press Ctrl+, -- preferences panel opens showing current project info
3. Press Escape -- panel closes
4. Press Ctrl+? -- cheatsheet shows Ctrl+, entry
</verification>

<success_criteria>
Ctrl+, opens a preferences panel. Panel shows project settings. Cheatsheet updated.
</success_criteria>

<output>
After completion, create `.planning/phases/08-keyboard-polish/08-08-SUMMARY.md`
</output>
