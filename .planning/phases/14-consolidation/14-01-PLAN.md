---
wave: 1
depends_on: []
requirements_addressed: [CONS-01, CONS-02]
files_modified:
  - src/tokens.ts
  - src/state-manager.ts
  - src/theme/theme-manager.ts
  - src/server/server-bridge.ts
  - src/components/sidebar.tsx
  - src/components/file-tree.tsx
  - src/components/gsd-viewer.tsx
  - src/components/server-pane.tsx
  - src/components/terminal-tabs.tsx
  - src/terminal/terminal-manager.ts
autonomous: true
---

<objective>
Remove dead code and tighten type safety across TypeScript source files. Categories: (1) unused exports detected via tsc --noEmit, (2) Arrow.js migration comment cleanup, (3) explicit return types on exported functions. Test suite guards against regressions.
</objective>

<tasks>

<task>
<read_first>
- CLAUDE.md §"Version Matrix"
- src/tokens.ts
</read_first>
<action>
Run: `pnpm exec tsc --noEmit 2>&1 | grep "is declared but never used"` to identify unused exports.

For each unused export found:
1. Grep across all TS files in src/ to confirm zero imports (exclude .test.ts files)
2. If zero non-test imports: remove the export from the source file
3. If the symbol is used in test files only: keep it (tests depend on implementation details)

Record findings in: .planning/phases/14-consolidation/dead-code-report.md with format:
```
## Unused Exports Found
- file:line: symbol_name (imports: N)
```

tsc output example that matches: `src/tokens.ts:13:5: 'FONTSIZE_SMALL' is declared but never used.`
</action>
<acceptance_criteria>
- dead-code-report.md exists with list of scanned files and findings
- `pnpm exec tsc --noEmit 2>&1 | grep "is declared but never used" | wc -l` returns 0 OR findings documented with rationale for keeping each
- No removed export causes `pnpm test` to fail (run tests after each removal)
</acceptance_criteria>
</task>

<task>
<read_first>
- src/tokens.ts
- src/state-manager.ts
- src/theme/theme-manager.ts
- src/server/server-bridge.ts
</read_first>
<action>
Scan for Arrow.js migration comment markers:

grep -rn "Migrated from Arrow.js" src/ --include="*.tsx" --include="*.ts"

For each file containing the comment:
1. Read the file
2. If the comment is only cosmetic (Phase 6.1 migration header) AND no live Arrow.js code: remove the comment line
3. If code logic was genuinely from Arrow.js and now Preact: keep comment as historical record

Files expected to have these comments (from prior scan):
- src/components/file-tree.tsx
- src/components/gsd-viewer.tsx
- src/components/tab-bar.tsx
- src/components/right-panel.tsx
- src/components/main-panel.tsx

Remove only the header comment `// Migrated from Arrow.js to Preact TSX (Phase 6.1)` — do not remove other comments or logic.
</action>
<acceptance_criteria>
- `grep -rn "Migrated from Arrow.js" src/ --include="*.tsx" --include="*.ts" | wc -l` returns 0 OR reasons documented for keeping each
- No removal changes any runtime behavior (comment-only change)
- `pnpm test` still passes after removals
</acceptance_criteria>
</task>

<task>
<read_first>
- src/tokens.ts
- src/state-manager.ts
- src/theme/theme-manager.ts
- src/server/server-bridge.ts
- src/terminal/terminal-manager.ts
</read_first>
<action>
Scan exported functions for missing explicit return types:

grep -n "export function" src/tokens.ts src/state-manager.ts src/theme/theme-manager.ts src/server/server-bridge.ts src/terminal/terminal-manager.ts

For each exported function WITHOUT an explicit return type annotation:
1. Add the return type based on what the function returns:
   - async functions returning Promise<T>: add `: Promise<T>`
   - sync functions returning signal or value: infer from body
2. Use `void` for functions with no return statement
3. Do NOT refactor function logic — only add the type annotation

Example pattern to add:
```typescript
// Before
export function getTheme(): ChromeTheme { ... }

// After
export function getTheme(): ChromeTheme { ... }
```
</action>
<acceptance_criteria>
- `grep -n "export function" src/tokens.ts src/state-manager.ts src/theme/theme-manager.ts src/server/server-bridge.ts src/terminal/terminal-manager.ts | grep -v ": " | wc -l` returns 0
- `pnpm exec tsc --noEmit 2>&1 | grep "error TS" | wc -l` returns 0
- `pnpm test` passes after type additions
</acceptance_criteria>
</task>

<task>
<read_first>
- vitest.config.ts
- vitest.setup.ts
- src/__test__/canary.test.ts
</read_first>
<action>
Run test suite to verify no regressions from dead code removal and type changes:

1. `pnpm test` — run all TypeScript tests
2. `pnpm test:coverage` — run with coverage to confirm baseline maintained

If any test fails:
1. Identify which change caused the failure
2. Revert that specific change
3. Document in dead-code-report.md as "reverted: reason"
4. Re-run tests to confirm pass
</action>
<acceptance_criteria>
- `pnpm test` exits with code 0
- Coverage report generated (even if not at threshold — this is post-consolidation baseline)
- No test file was modified to make tests pass (only source code changes allowed)
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `pnpm exec tsc --noEmit 2>&1` — must exit with code 0
- Run `pnpm test` — must exit with code 0
- `grep -rn "Migrated from Arrow.js" src/ --include="*.tsx" --include="*.ts" | wc -l` must equal 0 OR documented reasons
- No `any` type annotations added (grep pattern: `: any` or `as any` in non-test files)
</verification>

<must_haves>
1. Zero unused exports (tsc --noEmit clean)
2. Zero Arrow.js migration comments remaining
3. All exported functions have explicit return types
4. `pnpm test` passes (test suite guard intact)
5. Changes documented in dead-code-report.md
</must_haves>