---
phase: 10
plan: '07'
type: execute
wave: 2
depends_on: ['10-01']
files_modified:
  - src/components/server-pane.tsx
autonomous: true
requirements:
  - UI-01
  - UI-04
  - AGENT-01

must_haves:
  truths:
    - "server-pane.tsx strip state uses navy-blue palette (bgBase, bgBorder, status colors)"
    - "Expanded toolbar uses accentMuted for Start/Stop/Restart buttons, bgBorder for toggle/clear"
    - "Log area uses bgDeep (bgTerminal) background with proper text colors"
    - "Per-project cache, event listeners, button handlers all preserved"
  artifacts:
    - path: "src/components/server-pane.tsx"
      provides: "Server pane with strip/expanded states, ANSI log viewer, status dot"
      contains: "colors.statusGreen"
---

<objective>
Rewrite server-pane.tsx (the most complex state machine component) with navy-blue palette while preserving all existing logic (per-project cache, server lifecycle, event listeners, crash detection).

The reference has only a ServerPaneStrip (collapsed view). The production has both strip and expanded views. Adapt the reference strip pattern to production, update the expanded view toolbar and log area colors.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/server-pane.tsx (current — full implementation, keep all logic)
@.planning/phases/10-pixel-perfect-ui-rewrite/10-CONTEXT.md (D-08: restyle ServerPaneStrip for collapsed, existing expanded for expanded)
@.planning/phases/10-pixel-perfect-ui-rewrite/10-RESEARCH.md (Pitfall 2: server pane state machine)

# Key server-pane.tsx patterns to update
- Status dot: use colors.statusGreen (#3FB950) for running, colors.diffRed (#F85149) for crashed, textDim for stopped
- Toolbar: bg-bg-raised → bgElevated (#19243A), border-border → bgBorder (#243352)
- Start/Restart buttons: bg-accent/[0.125] text-accent → use inline style with colors.accentMuted + colors.accent
- Clear/Toggle buttons: border-border-interactive → bgBorder (#243352) border, textText → colors.textMuted
- Log area: bg-bg-terminal (bgDeep #0B1120) already correct
- Log text: text-text → colors.textMuted
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite server-pane.tsx with navy-blue palette</name>
  <files>src/components/server-pane.tsx</files>
  <read_first>src/components/server-pane.tsx (full implementation — understand per-project cache, button handlers, event listeners)</read_first>
  <action>
Rewrite `src/components/server-pane.tsx` visual styles to use navy-blue palette while preserving ALL existing logic (signals, per-project cache, event listeners, button handlers, crash detection).

**Read first:** Read the ENTIRE current `src/components/server-pane.tsx` to understand the component structure, signals, event listeners, button handlers, and state machine.

**Changes to apply:**

1. **Add tokens import:**
   `import { colors, fonts } from '../tokens';`

2. **Update dotColor logic (lines 123-127):**
   ```tsx
   const dotColor = status === 'running' ? colors.statusGreen :
                    status === 'crashed' ? colors.diffRed :
                    'transparent';
   // Stopped/unconfigured: transparent dot (opacity 0.4 below handles visibility)
   const dotOpacity = (status === 'stopped' || status === 'unconfigured') ? '0.4' : '1';
   ```
   Replace current hardcoded `#859900` (running) and `#dc322f` (crashed).

3. **Update toolbar wrapper (line 301):**
   - Replace `class="flex items-center gap-2 min-w-0 overflow-hidden flex-1 bg-bg px-3 py-1 border-t border-border"`
   - With `style={{ display: 'flex', alignItems: 'center', gap: 8, overflow: 'hidden', flex: 1, backgroundColor: colors.bgBase, padding: '4px 12px', borderTop: \`1px solid ${colors.bgBorder}\` }}`

4. **Update status text:**
   - `text-text` → `style={{ color: colors.textMuted, fontFamily: fonts.mono, fontSize: 11 }}`

5. **Update toggle button (line 309):**
   - Replace `class="rounded border border-border-interactive px-2 py-[2px] text-[10px] font-mono text-text hover:bg-bg-raised"`
   - With `style={{ borderRadius: 4, border: \`1px solid ${colors.bgBorder}\`, padding: '2px 8px', fontSize: 10, fontFamily: fonts.mono, color: colors.textMuted, backgroundColor: 'transparent' }}`

6. **Update button container (line 314):**
   - Replace `class="flex gap-1.5 items-center px-3 py-1 border-t border-border"`
   - With `style={{ display: 'flex', gap: 6, alignItems: 'center', padding: '4px 12px', borderTop: \`1px solid ${colors.bgBorder}\`, backgroundColor: colors.bgBase }}`

7. **Update Start button (line 321):**
   - Replace `class="rounded bg-accent/[0.125] px-2 py-[2px] text-[10px] font-mono text-accent hover:bg-accent/[0.25]"`
   - With `style={{ borderRadius: 4, backgroundColor: colors.accentMuted, border: 'none', padding: '2px 8px', fontSize: 10, fontFamily: fonts.mono, color: colors.accent, cursor: 'pointer' }}`
   - Same update for Stop, Restart, Open buttons (all use same `bg-accent/[0.125] text-accent` pattern)

8. **Update Clear button:**
   - Replace `class="rounded border border-border-interactive px-2 py-[2px] text-[10px] font-mono text-text hover:bg-bg-raised"`
   - With `style={{ borderRadius: 4, border: \`1px solid ${colors.bgBorder}\`, padding: '2px 8px', fontSize: 10, fontFamily: fonts.mono, color: colors.textMuted, backgroundColor: 'transparent' }}`

9. **Update log area (line 348):**
   - The `server-pane-logs` class already uses `--color-bg-raised` for background in app.css. Keep the class reference.
   - Update log text color in the `<span>` (line 350): `text-text` → `style={{ color: colors.textMuted }}`

10. **Update "No server command" message:**
    - `text-text` → `style={{ color: colors.textMuted }}`

**IMPORTANT:** Keep all existing signals (serverPaneState, serverStatus, detectedUrl, serverLogs), keep `projectServerCache` Map, keep `saveCurrentProjectState`/`restoreProjectState` functions, keep all event listeners (server-output, server-stopped), keep all button handlers (handleStart/Stop/Restart/Open/Toggle). Only update visual styles.

The `.server-pane-toolbar` CSS class in app.css uses `--color-bg-raised` which we updated to #19243A (bgElevated). The toolbar background should now be bgElevated.
  </action>
  <verify>
    <automated>grep -n "colors.statusGreen\|colors.diffRed\|colors.accentMuted\|colors.bgBase\|colors.bgBorder" src/components/server-pane.tsx | head -10</automated>
  </verify>
  <done>server-pane.tsx uses tokens.ts colors (statusGreen, diffRed, accentMuted, bgBase, bgBorder, textMuted). All logic preserved (per-project cache, server lifecycle, event listeners).</done>
</task>

</tasks>

<verification>
Run `pnpm build` to verify server-pane compiles correctly.
</verification>

<success_criteria>
- [ ] server-pane.tsx status dot uses colors.statusGreen (running) and colors.diffRed (crashed)
- [ ] Toolbar uses bgBase (#111927), bgBorder (#243352)
- [ ] Action buttons (Start/Stop/Restart/Open) use accentMuted background with accent text
- [ ] Clear and toggle buttons use bgBorder border with textMuted text
- [ ] All signals and logic preserved (serverPaneState, serverStatus, per-project cache, event listeners)
- [ ] pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-pixel-perfect-ui-rewrite/10-07-SUMMARY.md`
</output>