---
quick_id: 260410-vc7
slug: ui-fixes-left-sidebar-padding-tui-newlin
description: UI fixes: left sidebar padding, TUI newlines, pane backgrounds, git pane font size
date: 2026-04-10
mode: quick
---

# Quick Plan: 260410-vc7

## Task: UI Fixes

### 1. Left sidebar project tabs padding
**File:** `src/components/sidebar.tsx`
**Action:** Change `ProjectRow` padding from `10px 16px` with large margins to `10px 2px` (2px left padding as requested)
**Verify:** Sidebar project rows have minimal left padding
**Done:** ✓

### 2. TUI extra newlines in error handlers
**File:** `src/components/terminal-tabs.tsx`
**Action:** Remove `\r\n` prefix from all `writeln` calls in PTY error handlers (5 locations)
**Verify:** Error messages appear without double line breaks on session start
**Done:** ✓

### 3. Server pane default background color
**File:** `src/styles/app.css`
**Action:** Add `background: var(--color-bg)` to `.server-pane-logs` (defaults to #111927)
**Verify:** Server pane has dark background matching theme
**Done:** ✓

### 4. Git changes pane font sizes and padding
**Files:** `src/components/diff-viewer.tsx`
**Action:** Increase header font from 12px to 14px, content from 12px to 13px, reduce left padding from 16px to 2px
**Verify:** Diff content is more readable with larger fonts and minimal padding
**Done:** ✓
