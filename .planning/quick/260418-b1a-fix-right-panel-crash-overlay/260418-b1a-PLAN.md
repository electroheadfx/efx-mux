---
phase: 260418-b1a-fix-right-panel-crash-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/right-panel.tsx
autonomous: false
requirements:
  - QUICK-260418-b1a
must_haves:
  truths:
    - "When a right-scope agent/terminal tab's PTY exits (exit code 0), user sees the green 'Session ended' overlay with Restart button over the right panel's terminal area."
    - "When a right-scope agent/terminal tab's PTY crashes (non-zero exit), user sees the red 'Process crashed' overlay with exit code and Restart button."
    - "Clicking Restart on the right-scope overlay spawns a fresh PTY for that tab (session suffix becomes -rr<N>), same behavior as main scope."
    - "The overlay only appears while a dynamic terminal/agent tab is the active right-scope tab — not while File Tree, GSD, Git Changes, or an editor tab is active."
    - "Main-panel crash overlay behavior is unchanged (no regression from this fix)."
  artifacts:
    - path: "src/components/right-panel.tsx"
      provides: "RightPanel renders scope='right' ActiveTabCrashOverlay over the terminal-containers wrapper, guarded by isDynamic."
      contains: "ActiveTabCrashOverlay"
  key_links:
    - from: "src/components/right-panel.tsx"
      to: "getTerminalScope('right').ActiveTabCrashOverlay"
      via: "destructured helper called inside right-panel-content, rendered when isDynamic is true"
      pattern: "ActiveTabCrashOverlay"
    - from: "src/components/right-panel.tsx"
      to: "src/components/terminal-tabs.tsx (pty-exited listener → tab.exitCode)"
      via: "reactive read of scope state via ActiveTabCrashOverlayScoped('right')"
      pattern: "getTerminalScope\\('right'\\)"
---

<objective>
Fix the Phase-20 regression where right-scope agent/terminal tabs show tmux's raw `[exited]` buffer with no Restart affordance when their PTY exits. Mount the scoped `ActiveTabCrashOverlay` for scope='right' inside `src/components/right-panel.tsx`, mirroring the main-panel pattern.

Purpose: Restore parity between main-panel and right-panel terminal UX. Dead right-scope terminals must offer the same Restart button main-scope ones already do.
Output: Updated `src/components/right-panel.tsx` that renders the right-scope crash overlay over the terminal-containers wrapper while a dynamic terminal/agent tab is active.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/terminal-exited-no-restart.md
@src/components/right-panel.tsx
@src/components/main-panel.tsx
@src/components/terminal-tabs.tsx
@src/components/crash-overlay.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from terminal-tabs.tsx. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/components/terminal-tabs.tsx (line 911-938):
```typescript
export function getTerminalScope(scope: TerminalScope): {
  tabs: Signal<TerminalTab[]>;
  activeTabId: Signal<string | null>;
  // …other scoped helpers…
  ActiveTabCrashOverlay: () => JSX.Element | null;  // ← the one we need
};
```

From src/components/terminal-tabs.tsx (line 732-743) — what the helper actually renders:
```typescript
function ActiveTabCrashOverlayScoped(scope: TerminalScope) {
  const s = getScope(scope);
  const tab = s.tabs.value.find(t => t.id === s.activeTabId.value);
  if (!tab || tab.exitCode === undefined || tab.exitCode === null) return null;
  return <CrashOverlay tab={tab} onRestart={() => restartTabSession(tab.id)} />;
}
```

Reference pattern from src/components/main-panel.tsx (lines 8, 36):
```tsx
import { ActiveTabCrashOverlay } from './terminal-tabs';  // main-scope convenience export
// …
<div class="terminal-containers absolute inset-0" />
<ActiveTabCrashOverlay />  // mounted inside the terminal area wrapper
```

The right panel must use the SCOPED form (destructured from `getTerminalScope('right')`), NOT the top-level `ActiveTabCrashOverlay` export (which is hard-coded to 'main' per line 869).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount scope='right' ActiveTabCrashOverlay in RightPanel</name>
  <files>src/components/right-panel.tsx</files>
  <behavior>
    - When the active right-scope tab has `exitCode === 0`, the green "Session ended" overlay with Restart button renders inside `.right-panel-content`.
    - When the active right-scope tab has `exitCode !== 0` (e.g., 130 for Ctrl+C, 1 for generic crash), the red "Process crashed" overlay with exit code renders.
    - When `tab.exitCode` is undefined/null, no overlay renders.
    - Overlay is only reachable when `isDynamic` is true (File Tree / GSD / Git Changes / editor active tabs do NOT show the overlay).
    - Main-panel behavior is unchanged (scope='main' still renders its own overlay via the existing export).
  </behavior>
  <action>
    Edit `src/components/right-panel.tsx`:

    1. Update the destructuring at line 29 to also pull `ActiveTabCrashOverlay` from the scoped helper, aliased to disambiguate from the top-level main-scoped export name:
       ```tsx
       const { activeTabId, ActiveTabCrashOverlay: RightCrashOverlay } = getTerminalScope('right');
       ```

    2. Inside the `right-panel-content` div, as a sibling of the `.terminal-containers` wrapper (lines 101-109), add the overlay mount guarded by `isDynamic` so it only surfaces while a terminal/agent tab is the active right-scope tab:
       ```tsx
       {isDynamic && <RightCrashOverlay />}
       ```
       Place it AFTER the terminal-containers div so it layers on top (CrashOverlay uses `position: absolute; inset: 0; z-20` internally — see crash-overlay.tsx line 22 — which already handles stacking).

    3. Do NOT refactor AgentHeader scoping. That is a separately tracked follow-up per the diagnosis file's "Resolution" notes.

    4. Do NOT change main-panel.tsx — main scope's overlay mount is already correct.

    5. Do NOT change terminal-tabs.tsx — the scoped helper (line 732-743, exported line 936) already exists and works; it just needs a call site in the right panel.

    Rationale (why RightCrashOverlay alias): `getTerminalScope('right').ActiveTabCrashOverlay` returns a function that calls `ActiveTabCrashOverlayScoped('right')`. Aliasing it locally avoids any chance of shadowing the top-level `ActiveTabCrashOverlay` import in future edits and makes the scope explicit at the JSX call site.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run src/components/right-panel</automated>
  </verify>
  <done>
    - `src/components/right-panel.tsx` destructures `ActiveTabCrashOverlay` (aliased) from `getTerminalScope('right')`.
    - `<RightCrashOverlay />` (or equivalent) is rendered inside `right-panel-content`, guarded by `{isDynamic && ...}`.
    - `pnpm tsc --noEmit` passes with zero errors.
    - No other files are modified.
    - Main-panel.tsx and terminal-tabs.tsx are untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies right-panel crash overlay end-to-end</name>
  <what-built>RightPanel now mounts scope='right' ActiveTabCrashOverlay, so right-scope agent/terminal tabs show the Restart overlay when their PTY exits — matching main-panel behavior.</what-built>
  <how-to-verify>
    Run the app (the developer runs the server themselves per project policy — do not start it from Claude). Then:

    1. **Normal-exit overlay (green dot)**:
       - Open a project with the right panel enabled.
       - In the right-scope tab bar, create a new Terminal tab (plain shell) OR an Agent tab.
       - Type `exit` in the terminal and press Enter. For agents, quit the agent (e.g., `/exit` in claude) then type `exit` twice in the underlying shell.
       - **Expected:** Within ~1s, a centered overlay appears over the terminal area with a green dot, "Session ended" text, and a blue "Restart Session" button. No "[exited]" raw tmux text should be the only thing visible.

    2. **Crash overlay (red dot)**:
       - Create another right-scope Terminal tab.
       - In the terminal, run `kill -9 $$` (kills the current shell with SIGKILL → non-zero exit code).
       - **Expected:** Red dot, "Process crashed" text, "Terminal session ended (exit code 137)" (or similar non-zero), Restart button.

    3. **Restart works**:
       - From either overlay above, click "Restart Session".
       - **Expected:** Overlay disappears, a fresh tmux session attaches (the tab's session name gets an `-rr1` / `-rr2` suffix), terminal is interactive again.

    4. **Overlay is scoped correctly (no bleed to File Tree / GSD / editor tabs)**:
       - With a dead right-scope terminal tab, switch the right panel to File Tree, GSD, Git Changes (if owned by right), or a right-scope editor tab.
       - **Expected:** Overlay is hidden for all of those (it is guarded by `isDynamic`). Switching back to the dead terminal tab reveals the overlay again.

    5. **Main-panel not regressed**:
       - In the main panel, create an Agent tab, quit the agent, let the shell exit.
       - **Expected:** Main-panel overlay still renders exactly as before (green dot, Restart button). No visual or behavioral change in main scope.

    6. **Quit + reopen persistence**:
       - With a dead right-scope terminal tab visible (overlay showing), quit the app (Cmd+Q) and reopen.
       - **Expected:** Tab is restored. If its session is still dead (or the restored agent immediately re-exits), the overlay re-appears on the restored tab. Clicking Restart works.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which one failed with exact observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- Static: `pnpm tsc --noEmit` passes.
- Unit: Existing right-panel component tests (if any) still pass; `pnpm vitest run src/components/right-panel` green.
- Functional: 6-step human verification above (Task 2) fully passes.
- Scope: Diff is limited to `src/components/right-panel.tsx` (one file, small additive change).
</verification>

<success_criteria>
- Right-scope agent/terminal tabs show the crash overlay with Restart button on PTY exit (both normal and crash paths).
- Overlay is hidden when a non-terminal right-scope tab (File Tree / GSD / Git Changes / editor) is active.
- Restart button spawns a fresh PTY for the right-scope tab.
- Main-panel crash overlay behavior is unchanged.
- Fix touches only `src/components/right-panel.tsx`.
- No Rust changes; no changes to terminal-tabs.tsx or main-panel.tsx.
</success_criteria>

<output>
After completion, create `.planning/quick/260418-b1a-fix-right-panel-crash-overlay/260418-b1a-SUMMARY.md` capturing:
- Exact diff applied (before/after JSX snippet for right-panel.tsx).
- Verification checklist results from Task 2 (all 6 steps).
- Note the deferred follow-up: scoping AgentHeader for right-scope agent tabs (Ready/Stopped pill) — NOT done in this quick task.
</output>
