---
phase: quick-260411-dz7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/preferences-panel.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Preferences panel opens without a Theme toggle or APPEARANCE section"
    - "All other preferences sections (CURRENT PROJECT, FILE TREE, SHORTCUTS, ACTIONS) remain intact"
    - "No TypeScript compilation errors"
  artifacts:
    - path: "src/components/preferences-panel.tsx"
      provides: "Preferences panel without dead theme toggle"
  key_links: []
---

<objective>
Remove the non-functional light/dark theme toggle from the preferences panel.

Purpose: The ThemeToggle and APPEARANCE section in preferences-panel.tsx do nothing useful because all UI colors come from hardcoded tokens in src/tokens.ts via inline styles, bypassing the CSS custom properties that the toggle switches. Removing dead UI prevents user confusion.

Output: Clean preferences-panel.tsx without theme toggle artifacts.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/preferences-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ThemeToggle component and APPEARANCE section from preferences panel</name>
  <files>src/components/preferences-panel.tsx</files>
  <action>
In src/components/preferences-panel.tsx, make these removals:

1. Remove the `toggleThemeMode` import from line 8. The import line currently reads:
   `import { toggleThemeMode } from '../theme/theme-manager';`
   Delete this entire line.

2. Remove the entire `ThemeToggle` component (lines ~138-181). This is the function that renders Dark/Light toggle buttons.

3. Remove the `isDark` variable (line ~207):
   `const isDark = document.documentElement.getAttribute('data-theme') !== 'light';`

4. Remove the APPEARANCE section in the JSX body (lines ~318-322). This is two elements:
   - `<SectionLabel label="APPEARANCE" />`
   - The `<SettingRow label="Theme">` with its `<ThemeToggle value={isDark} />` child

Do NOT touch:
- theme-manager.ts (keep toggleThemeMode export there for Ctrl+Shift+T in main.tsx)
- app.css light mode definitions
- Any other section in preferences-panel.tsx
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && pnpm exec tsc --noEmit 2>&1 | head -20 && ! grep -q "ThemeToggle\|toggleThemeMode\|isDark\|APPEARANCE" src/components/preferences-panel.tsx && echo "CLEAN: no theme toggle references remain"</automated>
  </verify>
  <done>
- preferences-panel.tsx compiles without errors
- No references to ThemeToggle, toggleThemeMode, isDark, or APPEARANCE in the file
- All other sections (CURRENT PROJECT, FILE TREE, SHORTCUTS, ACTIONS) unchanged
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes
- `grep -c "ThemeToggle\|APPEARANCE\|isDark" src/components/preferences-panel.tsx` returns 0
- `grep -c "toggleThemeMode" src/components/preferences-panel.tsx` returns 0
- `grep -c "CURRENT PROJECT\|FILE TREE\|SHORTCUTS\|ACTIONS" src/components/preferences-panel.tsx` returns 4 (all other sections intact)
</verification>

<success_criteria>
Preferences panel renders without any theme toggle UI. All other preferences sections remain functional. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-dz7-remove-non-functional-light-dark-theme-t/260411-dz7-SUMMARY.md`
</output>
