---
status: complete
quick_id: 260416-j71
date: 2026-04-16
---

# Quick Task 260416-j71: File tab pinning

## What Changed

### Task 1: Pin state + replaceable-pane logic (unified-tab-bar.tsx)
- Added `pinned` boolean field to `EditorTabData` interface
- `openEditorTab()` now replaces the first unpinned editor tab instead of always creating new
- Added `openEditorTabPinned()` for opening files directly as pinned
- Added `togglePinTab()`, visual indicators (italic = preview, pin icon = pinned)
- Double-click on tab header pins it; right-click toggles pin state
- Backward-compatible persistence (old tabs default to unpinned)

### Task 2: File-tree click wiring (file-tree.tsx + main.tsx)
- 250ms timer-based click discrimination: single-click = preview (replaceable), double-click = pinned
- `file-opened-pinned` custom event dispatched on double-click
- main.tsx handles `file-opened-pinned` event via `openEditorTabPinned()`

## Commits
- `efc91f7` feat(quick-260416-j71): add pin state and replaceable-pane logic to unified-tab-bar
- `aeef99e` feat(quick-260416-j71): wire file-tree single-click and double-click events

## Files Modified
- src/components/unified-tab-bar.tsx
- src/components/file-tree.tsx
- src/main.tsx
