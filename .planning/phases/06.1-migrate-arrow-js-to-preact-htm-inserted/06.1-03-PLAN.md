---
phase: 06.1-migrate-arrow-js-to-preact-htm-inserted
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - src/components/tab-bar.tsx
  - src/components/main-panel.tsx
  - src/components/diff-viewer.tsx
  - src/components/gsd-viewer.tsx
  - src/components/file-tree.tsx
autonomous: true
requirements:
  - PRESERVE-ALL
  - SC-01
  - SC-03
  - SC-04
  - SC-05
must_haves:
  truths:
    - "TabBar renders tabs with active state highlighting using Tailwind classes"
    - "MainPanel renders terminal-area and file viewer overlay with escape-to-close"
    - "DiffViewer renders syntax-highlighted git diffs"
    - "GSDViewer renders markdown with checkbox write-back and auto-refresh on md-file-changed"
    - "FileTree renders keyboard-navigable directory listing"
  artifacts:
    - path: "src/components/tab-bar.tsx"
      provides: "Reusable tab bar Preact component"
      contains: "export function TabBar"
    - path: "src/components/main-panel.tsx"
      provides: "Main panel with terminal area and file viewer"
      contains: "export function MainPanel"
    - path: "src/components/diff-viewer.tsx"
      provides: "Git diff viewer"
      contains: "export function DiffViewer"
    - path: "src/components/gsd-viewer.tsx"
      provides: "GSD markdown viewer with checkbox write-back"
      contains: "export function GSDViewer"
    - path: "src/components/file-tree.tsx"
      provides: "Keyboard-navigable file tree"
      contains: "export function FileTree"
  key_links:
    - from: "src/components/gsd-viewer.tsx"
      to: "@tauri-apps/api/core"
      via: "invoke for read_file_content and write_checkbox"
      pattern: "invoke\\("
    - from: "src/components/gsd-viewer.tsx"
      to: "@tauri-apps/api/event"
      via: "listen for md-file-changed"
      pattern: "listen\\("
---

<objective>
Convert 5 simpler UI components (tab-bar, main-panel, diff-viewer, gsd-viewer, file-tree) from Arrow.js to Preact TSX function components with Tailwind utility classes.

Purpose: These are the leaf/mid-level components. Tab-bar is consumed by right-panel.tsx (Plan 04). The others are self-contained viewers that manage their own state. Converting them in parallel with the complex components (Plan 04) maximizes execution throughput.

Output: 5 TSX files that export Preact function components with typed props, useRef/useEffect for DOM-imperative code, and Tailwind classes replacing inline styles.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-CONTEXT.md
@.planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-RESEARCH.md
@.planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-02-SUMMARY.md

<interfaces>
<!-- From state-manager.ts (Plan 02 output) -->
export const projects: Signal<ProjectEntry[]>;
export const activeProjectName: Signal<string | null>;
export const sidebarCollapsed: Signal<boolean>;
export const rightTopTab: Signal<string>;
export const rightBottomTab: Signal<string>;
export interface ProjectEntry { path: string; name: string; agent: string; gsd_file?: string; server_cmd?: string; }
export interface GitData { branch: string; modified: number; staged: number; untracked: number; }
export async function getProjects(): Promise<ProjectEntry[]>;
export async function getActiveProject(): Promise<string | null>;
export async function getGitStatus(path: string): Promise<GitData>;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert tab-bar.tsx and main-panel.tsx</name>
  <files>src/components/tab-bar.tsx, src/components/main-panel.tsx</files>
  <read_first>
    - src/components/tab-bar.js (21 lines -- simplest component)
    - src/components/main-panel.js (133 lines -- terminal area + file viewer overlay)
    - src/styles/app.css (verify Tailwind theme token names for utility classes)
    - .planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-RESEARCH.md (Pattern 2: html template -> JSX, Pattern 4: DOM-imperative in Preact)
  </read_first>
  <action>
**src/components/tab-bar.tsx** (per D-05, D-06, D-07):
```typescript
import type { Signal } from '@preact/signals';

interface TabBarProps {
  tabs: string[];
  activeTab: Signal<string>;
  onSwitch: (tab: string) => void;
}

export function TabBar({ tabs, activeTab, onSwitch }: TabBarProps) {
  return (
    <div class="flex gap-0.5 px-2 pt-1 bg-bg border-b border-border shrink-0">
      {tabs.map(tab => (
        <button
          class={activeTab.value === tab
            ? 'px-4 py-1.5 text-xs cursor-pointer font-[inherit] bg-bg-raised border-b-2 border-accent text-text-bright -mb-px'
            : 'px-4 py-1.5 text-xs cursor-pointer font-[inherit] bg-transparent border-b-2 border-transparent text-text -mb-px hover:bg-bg-raised'}
          onClick={() => onSwitch(tab)}
        >{tab}</button>
      ))}
    </div>
  );
}
```

Key migration points:
- Arrow.js `@click` -> Preact `onClick`
- Arrow.js `class="${() => ...}"` reactive expression -> direct ternary with `.value` access
- Inline style padding/gap replaced with Tailwind utilities
- Props typed with `Signal<string>` for activeTab (consumer passes signal directly for fine-grained updates per Research Anti-patterns)

**src/components/main-panel.tsx** (per D-05, D-06, D-07):
- Import `{ signal } from '@preact/signals'` and `{ useEffect } from 'preact/hooks'`
- Create module-level signals for file viewer state:
  ```typescript
  const fileViewerVisible = signal(false);
  const fileName = signal('');
  const filePath = signal('');
  const fileContent = signal('');
  ```
- Convert `document.addEventListener('show-file-viewer', ...)` to a `useEffect` that registers the listener and returns cleanup
- Convert `document.addEventListener('keydown', ...)` for Escape key similarly
- Keep the `escapeHtml()` helper function as-is (pure function, just add TS types)
- Export `MainPanel` as a function component returning JSX
- The `.terminal-area` div must keep its class name (terminal-manager.ts queries it by class) -- use `class="terminal-area flex-1 bg-bg overflow-hidden relative min-h-[100px] p-1"`
- File viewer overlay uses conditional rendering: `{fileViewerVisible.value && (<div class="...">...</div>)}`  (per D-07, SC-05 -- no Arrow.js `() =>` wrapper needed)
- Server pane placeholder kept as-is with Tailwind classes
- Replace all inline `style="..."` with equivalent Tailwind utility classes where possible. Keep `style` prop only for truly dynamic values or complex CSS that Tailwind cannot express.
- The split handle between terminal and server pane: `<div class="split-handle-h" data-handle="main-h" role="separator" ...>` must be preserved exactly (drag-manager.ts queries by data-handle)

Do NOT delete old .js files -- Plan 05 cleanup.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && grep -c "export function TabBar" src/components/tab-bar.tsx && grep -c "export function MainPanel" src/components/main-panel.tsx && grep -c "@arrow-js/core" src/components/tab-bar.tsx src/components/main-panel.tsx 2>/dev/null | grep -v ':0$' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - src/components/tab-bar.tsx exists and contains `export function TabBar`
    - src/components/tab-bar.tsx contains `interface TabBarProps`
    - src/components/tab-bar.tsx contains `Signal<string>` type for activeTab prop
    - src/components/tab-bar.tsx does NOT contain `@arrow-js/core`
    - src/components/main-panel.tsx exists and contains `export function MainPanel`
    - src/components/main-panel.tsx contains `signal(` for file viewer state
    - src/components/main-panel.tsx contains `fileViewerVisible.value` (conditional rendering per D-07)
    - src/components/main-panel.tsx contains `class="terminal-area`
    - src/components/main-panel.tsx contains `data-handle="main-h"` (drag manager integration)
    - Neither file contains `@arrow-js/core`
    - Neither file contains `window.__TAURI__`
  </acceptance_criteria>
  <done>tab-bar.tsx and main-panel.tsx converted to Preact TSX with typed props, Tailwind classes, and signal-based reactivity</done>
</task>

<task type="auto">
  <name>Task 2: Convert diff-viewer.tsx, gsd-viewer.tsx, and file-tree.tsx</name>
  <files>src/components/diff-viewer.tsx, src/components/gsd-viewer.tsx, src/components/file-tree.tsx</files>
  <read_first>
    - src/components/diff-viewer.js (105 lines)
    - src/components/gsd-viewer.js (155 lines)
    - src/components/file-tree.js (152 lines)
    - src/state-manager.ts (for signal imports -- activeProjectName, projects)
    - .planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-RESEARCH.md (Pattern 2, Pattern 4: DOM-imperative in Preact)
  </read_first>
  <action>
**src/components/diff-viewer.tsx:**
- Import `{ useRef, useEffect } from 'preact/hooks'` and `{ invoke } from '@tauri-apps/api/core'`
- Keep `escapeHtml()` and `renderDiffHtml()` as pure helper functions (add TS types to params/returns)
- Use `useRef<HTMLDivElement>(null)` for the content div instead of `document.getElementById('diff-viewer-content')`
- Register `document.addEventListener('open-diff', ...)` in a `useEffect` with cleanup return
- The `loadDiff` function reads `ref.current` instead of the old `contentEl` variable
- JSX renders `<div ref={contentRef}>` instead of `id="diff-viewer-content"`
- Tailwind classes for container: `class="h-full overflow-y-auto px-4 py-2 font-mono text-[13px] leading-relaxed"`
- Initial placeholder text: `"Click a file in the sidebar to view its diff"`

**src/components/gsd-viewer.tsx:**
- Import `{ useRef, useEffect } from 'preact/hooks'`, `{ invoke } from '@tauri-apps/api/core'`, `{ listen } from '@tauri-apps/api/event'`, `{ marked } from 'marked'`
- Import `{ activeProjectName, projects } from '../state-manager'` (signals, not function params)
- Props: no props needed -- reads activeProject directly from signals using `computed` or inline derivation
- Use `useRef<HTMLDivElement>(null)` for content div
- `useEffect` for:
  1. Configure marked renderer (checkbox override) -- run once
  2. Listen for `md-file-changed` Tauri event -- return unlisten cleanup
  3. Listen for `project-changed` DOM event -- return removeEventListener cleanup
  4. Initial load when component mounts (if active project exists)
- `buildLineMap()` and `injectLineNumbers()` helper functions preserved exactly (add TS types)
- Checkbox click handler via `onClick` on container div (event delegation, same pattern)
- Derive active project: `const project = projects.value.find(p => p.name === activeProjectName.value)`
- Tailwind classes for container: `class="h-full overflow-y-auto p-4"` with click handler

**src/components/file-tree.tsx:**
- Import `{ useRef, useEffect } from 'preact/hooks'`, `{ signal } from '@preact/signals'`, `{ invoke } from '@tauri-apps/api/core'`
- Import `{ activeProjectName, projects } from '../state-manager'`
- Local signals for component state: `entries`, `selectedIndex`, `currentPath`, `loaded`
- Derive active project from global signals (same as gsd-viewer)
- `useEffect` for: project-changed listener, initial load
- Keyboard handler as Preact `onKeyDown` prop on the container div
- Each entry rendered with conditional Tailwind classes for selected state
- File entry click dispatches `file-opened` CustomEvent (same behavior)
- Backspace guard: only navigate up if parent starts with project root path

All three components: Use `class` attribute (not `className`) per Preact convention. Replace all Arrow.js `() =>` reactive wrappers with direct signal `.value` access in JSX. Replace `@click` with `onClick`, `@mouseenter` with `onMouseEnter`, etc.

Do NOT delete old .js files -- Plan 05 cleanup.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && for f in src/components/diff-viewer.tsx src/components/gsd-viewer.tsx src/components/file-tree.tsx; do test -f "$f" && echo "OK: $f" || echo "MISSING: $f"; done && grep -c "@arrow-js/core" src/components/diff-viewer.tsx src/components/gsd-viewer.tsx src/components/file-tree.tsx 2>/dev/null | grep -v ':0$' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - src/components/diff-viewer.tsx exists and contains `export function DiffViewer`
    - src/components/diff-viewer.tsx contains `useRef<HTMLDivElement>`
    - src/components/diff-viewer.tsx contains `import { invoke } from '@tauri-apps/api/core'`
    - src/components/gsd-viewer.tsx exists and contains `export function GSDViewer`
    - src/components/gsd-viewer.tsx contains `import { listen } from '@tauri-apps/api/event'`
    - src/components/gsd-viewer.tsx contains `marked.parse` (markdown rendering preserved)
    - src/components/gsd-viewer.tsx contains `write_checkbox` (checkbox write-back preserved)
    - src/components/file-tree.tsx exists and contains `export function FileTree`
    - src/components/file-tree.tsx contains `file-opened` (CustomEvent dispatch preserved)
    - src/components/file-tree.tsx contains `onKeyDown` (keyboard navigation)
    - NONE of the 3 files contain `@arrow-js/core`
    - NONE of the 3 files contain `window.__TAURI__`
  </acceptance_criteria>
  <done>diff-viewer.tsx, gsd-viewer.tsx, and file-tree.tsx converted to Preact TSX with useRef/useEffect for DOM operations, signal-based state, and Tailwind classes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> marked.js | Markdown content rendered as HTML (XSS vector) |
| Checkbox click -> write_checkbox IPC | User can modify .md files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-03 | Tampering | gsd-viewer markdown rendering | mitigate | marked.js checkbox renderer is preserved from Phase 6; no new XSS vectors introduced |
| T-06.1-04 | Tampering | diff-viewer innerHTML | mitigate | escapeHtml() applied to all diff content before innerHTML assignment (preserved from Phase 6) |
</threat_model>

<verification>
1. All 5 .tsx files exist
2. No file imports from `@arrow-js/core`
3. No file uses `window.__TAURI__`
4. TabBar uses Signal<string> prop type
5. GSD viewer preserves checkbox write-back via invoke('write_checkbox')
6. File tree preserves keyboard navigation (onKeyDown with ArrowUp/Down/Enter/Backspace)
</verification>

<success_criteria>
- 5 Preact TSX components created with typed props and Tailwind classes
- All Arrow.js patterns replaced (reactive->signals, html``->JSX, @click->onClick)
- DOM-imperative code uses useRef + useEffect (not setTimeout + getElementById)
- Conditional rendering uses standard JSX patterns (SC-04, SC-05)
- All existing functionality preserved (checkbox write-back, auto-refresh, keyboard nav, diff highlighting)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-migrate-arrow-js-to-preact-htm-inserted/06.1-03-SUMMARY.md`
</output>
