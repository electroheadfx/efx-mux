---
phase: 09-professional-ui-overhaul
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/project-modal.tsx
  - src/components/preferences-panel.tsx
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Add Project modal has rounded-xl (12px) corners with shadow-2xl"
    - "Modal input fields have dark #0D1117 fill, #30363D border, 8px radius"
    - "Modal has visible header/footer dividers"
    - "Preferences panel has section-label styled headers"
    - "Preferences shows keycap-styled keyboard shortcut badges"
    - "Both modals render correctly and existing functionality is preserved"
  artifacts:
    - path: "src/components/project-modal.tsx"
      provides: "Restyled Add Project modal matching mockup"
      contains: "rounded-xl"
    - path: "src/components/preferences-panel.tsx"
      provides: "Restyled Preferences panel matching mockup"
      contains: "section-label"
  key_links:
    - from: "src/components/project-modal.tsx"
      to: "src/state-manager.ts"
      via: "addProject, updateProject, switchProject imports"
      pattern: "from '../state-manager'"
    - from: "src/components/preferences-panel.tsx"
      to: "src/theme/theme-manager.ts"
      via: "toggleThemeMode import"
      pattern: "from '../theme/theme-manager'"
---

<objective>
Restyle the Add Project modal and Preferences panel to match the Pencil mockup designs. Keep all existing logic unchanged -- only update Tailwind classes and minor JSX structure for visual refinement.

Purpose: Modals are high-visibility interactions that must feel polished. Professional form styling builds user confidence.
Output: Mockup-matching project modal and preferences panel.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-professional-ui-overhaul/09-CONTEXT.md
@.planning/phases/09-professional-ui-overhaul/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Add Project modal to match mockup (D-14)</name>
  <files>src/components/project-modal.tsx</files>
  <read_first>
    - src/components/project-modal.tsx (full file -- modal container, form fields, buttons, all event handlers)
    - .planning/phases/09-professional-ui-overhaul/09-CONTEXT.md (D-14 decision)
  </read_first>
  <action>
Use the Pencil MCP tool `get_screenshot` with node ID `JyvDG` to view the Add Project Modal mockup. Study exact spacing, corner radius, input styling, button layout.

Restyle project-modal.tsx -- keep ALL existing logic (signals, handlers, form submission, directory browse). Only update Tailwind classes.

1. Modal card container (currently `rounded-lg shadow-2xl`):
   Change to: `w-[480px] bg-bg-raised border border-border-interactive rounded-xl shadow-2xl z-[101]`
   - `rounded-xl` = 12px per D-14
   - `border-border-interactive` = #30363D (the interactive border, not subtle)

2. Modal header:
   ```
   flex items-center px-6 py-4 border-b border-border
   ```
   Title text: `text-base font-semibold text-text-bright font-sans`
   Close button: keep as-is (already styled)

3. All input fields -- update from current classes to:
   ```
   w-full h-9 px-3 text-sm bg-bg border border-border-interactive rounded-lg text-text-bright outline-none focus:border-accent transition-colors font-sans
   ```
   Key changes:
   - `h-8` -> `h-9` (slightly taller)
   - `px-2` -> `px-3` (more horizontal padding per D-14: 10px/12px padding)
   - `rounded-sm` -> `rounded-lg` (8px radius per D-14)
   - `bg-bg` stays (= #0D1117 dark fill per D-14)
   - `border-border` -> `border-border-interactive` (#30363D)
   - Add `font-sans` for Geist font

4. Directory input + Browse button combo:
   - Input: `flex-1 h-9 px-3 text-sm bg-bg border border-border-interactive rounded-l-lg text-text-bright outline-none focus:border-accent transition-colors font-sans`
   - Browse button: `w-9 h-9 bg-bg border border-border-interactive border-l-0 rounded-r-lg text-text cursor-pointer text-sm shrink-0 hover:text-text-bright hover:bg-bg-raised transition-colors`

5. Form labels: Add `section-label` class to replace current inline uppercase styling:
   ```
   <label class="section-label block mb-1.5">Directory</label>
   ```
   (section-label already provides: GeistMono 500, 10px, uppercase, letter-spacing 1.2px)

6. Footer buttons:
   - Cancel: `bg-transparent border border-border-interactive text-text px-4 py-2 rounded-lg cursor-pointer text-sm font-sans hover:text-text-bright hover:border-accent transition-colors`
   - Submit (Add Project): `bg-accent text-white px-4 py-2 rounded-lg text-sm font-sans cursor-pointer hover:opacity-90 transition-opacity disabled:opacity-40 disabled:cursor-not-allowed`
   - Add a footer divider: wrap buttons in a `px-6 py-4 border-t border-border flex justify-end gap-3`

7. Error message: use `text-danger` instead of any hardcoded red color.
  </action>
  <verify>
    <automated>grep -c "rounded-xl" src/components/project-modal.tsx && grep -c "rounded-lg" src/components/project-modal.tsx && grep -c "border-border-interactive" src/components/project-modal.tsx && grep -c "section-label" src/components/project-modal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - project-modal.tsx modal card uses `rounded-xl` (12px corners)
    - project-modal.tsx uses `shadow-2xl` on the modal card
    - project-modal.tsx input fields use `rounded-lg` (8px radius)
    - project-modal.tsx input fields use `bg-bg` (dark fill = #0D1117)
    - project-modal.tsx input fields use `border-border-interactive`
    - project-modal.tsx labels use `section-label` class
    - project-modal.tsx footer has `border-t border-border` divider
    - project-modal.tsx buttons use `rounded-lg`
    - project-modal.tsx uses `font-sans` on inputs and buttons
    - All existing form logic (browse, validate, submit, edit mode) unchanged
  </acceptance_criteria>
  <done>Add Project modal restyled with 12px rounded corners, 8px input radius, dark input fills, section-label headers, and footer divider.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Preferences panel to match mockup (D-15)</name>
  <files>src/components/preferences-panel.tsx</files>
  <read_first>
    - src/components/preferences-panel.tsx (full file -- sections, theme toggle, shortcuts, edit action)
    - .planning/phases/09-professional-ui-overhaul/09-CONTEXT.md (D-15 decision)
  </read_first>
  <action>
Use the Pencil MCP tool `get_screenshot` with node ID `Vwvx7` to view the Preferences Panel mockup. Study section layout, keycap styling, toggle design.

Restyle preferences-panel.tsx -- keep ALL existing logic. Only update Tailwind classes and minor JSX adjustments.

1. Overlay backdrop: keep `fixed inset-0 bg-black/30 z-[100] flex items-center justify-center`

2. Panel card: update to match modal style:
   ```
   w-[500px] max-h-[70vh] bg-bg-raised border border-border-interactive rounded-xl shadow-2xl overflow-y-auto
   ```
   Key: `rounded-lg` -> `rounded-xl`, add `border-border-interactive`

3. Header: `px-6 py-4 border-b border-border flex items-center justify-between`
   Title: `text-text-bright text-base font-semibold font-sans`

4. Section headers -- replace current inline uppercase classes with section-label:
   Current: `px-6 py-2 text-[11px] uppercase tracking-widest text-text`
   New: `px-6 py-3 section-label`
   Apply to: "Current Project", "Appearance", "Shortcuts", "Actions"

5. Setting rows: `flex items-center justify-between px-6 py-2.5 border-b border-border/30`
   Label: `text-sm text-text-bright font-sans`
   Value: `text-sm text-text font-mono`

6. Keyboard shortcut badges per D-15 -- keycap style:
   Replace current `inline-flex items-center px-2 py-0.5 bg-accent/15 text-accent text-xs font-mono rounded` with keycap style:
   ```
   inline-flex items-center px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded
   ```
   This gives the keycap appearance: dark fill (#0D1117) + border (#30363D) + bright text + 4px radius.

   Update the shortcuts section to show actual keybindings (not just "Ctrl+?"):
   ```tsx
   <div class="flex items-center justify-between px-6 py-2.5 border-b border-border/30">
     <span class="text-sm text-text font-sans">Toggle sidebar</span>
     <kbd class="inline-flex items-center gap-1 px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded">Ctrl+B</kbd>
   </div>
   <div class="flex items-center justify-between px-6 py-2.5 border-b border-border/30">
     <span class="text-sm text-text font-sans">Quick switch</span>
     <kbd class="inline-flex items-center gap-1 px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded">Ctrl+P</kbd>
   </div>
   <div class="flex items-center justify-between px-6 py-2.5 border-b border-border/30">
     <span class="text-sm text-text font-sans">New tab</span>
     <kbd class="inline-flex items-center gap-1 px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded">Ctrl+T</kbd>
   </div>
   <div class="flex items-center justify-between px-6 py-2.5 border-b border-border/30">
     <span class="text-sm text-text font-sans">Close tab</span>
     <kbd class="inline-flex items-center gap-1 px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded">{'\u2318'}+W</kbd>
   </div>
   <div class="flex items-center justify-between px-6 py-2.5 border-b border-border/30">
     <span class="text-sm text-text font-sans">All shortcuts</span>
     <kbd class="inline-flex items-center gap-1 px-2 py-0.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded">Ctrl+?</kbd>
   </div>
   ```

7. Theme toggle button: keep functionality, update style to be a proper toggle pill:
   ```
   inline-flex items-center gap-2 px-3 py-1.5 bg-bg border border-border-interactive text-text-bright text-xs font-mono rounded-lg cursor-pointer hover:border-accent transition-colors
   ```

8. Edit Project button in Actions section:
   ```
   bg-accent text-white px-4 py-2 rounded-lg text-sm font-sans cursor-pointer hover:opacity-90 transition-opacity
   ```
  </action>
  <verify>
    <automated>grep -c "section-label" src/components/preferences-panel.tsx && grep -c "rounded-xl" src/components/preferences-panel.tsx && grep -c "border-border-interactive" src/components/preferences-panel.tsx && grep -c "kbd" src/components/preferences-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - preferences-panel.tsx panel card uses `rounded-xl`
    - preferences-panel.tsx uses `border-border-interactive` on the card
    - preferences-panel.tsx section headers use `section-label` class
    - preferences-panel.tsx contains `<kbd` elements for keyboard shortcuts
    - preferences-panel.tsx shows at least 5 keyboard shortcut rows (Ctrl+B, Ctrl+P, Ctrl+T, Cmd+W, Ctrl+?)
    - preferences-panel.tsx keycap badges use `bg-bg border border-border-interactive` classes
    - preferences-panel.tsx theme toggle uses `font-mono` and `rounded-lg`
    - All existing logic (closePreferences, toggleThemeMode, openProjectModal) unchanged
  </acceptance_criteria>
  <done>Preferences panel restyled with section-label headers, keycap-styled shortcut badges, rounded-xl card, and professional setting rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal form inputs | User-entered project paths and names -- validated before submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | project-modal form data | accept | Existing validation (isValid computed) retained; Rust backend validates paths |
| T-09-08 | Information Disclosure | preferences-panel project path | accept | Displays user's own project path -- no external data exposure |
</threat_model>

<verification>
1. Open Add Project modal (+ button or first-run) -- see rounded corners, dark inputs, proper labels
2. Input fields have 8px radius with #30363D borders
3. Footer has visible divider line
4. Open Preferences (Ctrl+,) -- see section headers in uppercase Geist Mono
5. Keyboard shortcuts displayed as keycap badges
6. Theme toggle works and styling is consistent
7. Submit a project -- existing form logic works
</verification>

<success_criteria>
- Add Project modal matches Pencil mockup (JyvDG)
- Preferences panel matches Pencil mockup (Vwvx7)
- Keycap-styled keyboard shortcut badges visible
- All form functionality preserved (add, edit, browse, validate, theme toggle)
</success_criteria>

<output>
After completion, create `.planning/phases/09-professional-ui-overhaul/09-04-SUMMARY.md`
</output>
