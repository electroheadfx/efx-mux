---
phase: 06.1-migrate-arrow-js-to-preact-htm-inserted
plan: 02
subsystem: ui
tags: [preact, signals, typescript, tauri-api, xterm]

requires:
  - phase: 06.1-01
    provides: Vite build config with Preact/HTM aliases and @preact/signals dependency
provides:
  - Signal-based state-manager.ts with Preact signals for app-wide reactive state
  - TypeScript drag-manager with framework-agnostic DOM manipulation
  - TypeScript terminal-manager, pty-bridge, resize-handler with @tauri-apps/api imports
  - TypeScript theme-manager with @tauri-apps/api imports and typed interfaces
affects: [06.1-03, 06.1-04, 06.1-05]

tech-stack:
  added: [@preact/signals]
  patterns: [signal-based state, tauri-api-imports, typed-utility-modules]

key-files:
  created:
    - src/state-manager.ts
    - src/drag-manager.ts
    - src/terminal/terminal-manager.ts
    - src/terminal/pty-bridge.ts
    - src/terminal/resize-handler.ts
    - src/theme/theme-manager.ts
  modified: []

key-decisions:
  - "Keep currentState as internal mutable variable (not a signal) since it is the raw Rust state blob"
  - "Dispatch CustomEvent for backward compat with main.js project-changed listener alongside signal update"
  - "Use import type for xterm.js/fit-addon in modules that only need type information"

patterns-established:
  - "Pattern: import { invoke } from '@tauri-apps/api/core' replaces window.__TAURI__.core"
  - "Pattern: import { listen } from '@tauri-apps/api/event' replaces window.__TAURI__.event"
  - "Pattern: export const mySignal = signal<T>(default) for app-wide reactive state"

requirements-completed: [PRESERVE-ALL, SC-01, SC-02]

duration: 2min
completed: 2026-04-08
---

# Phase 06.1 Plan 02: State Manager and Utility Module TS Conversion Summary

**Signal-based state-manager.ts with @preact/signals and 5 utility modules converted from JS to TypeScript with @tauri-apps/api imports**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-08T13:56:32Z
- **Completed:** 2026-04-08T13:58:41Z
- **Tasks:** 2
- **Files created:** 6

## Accomplishments
- Created state-manager.ts with Preact signals (projects, activeProjectName, sidebarCollapsed, rightTopTab, rightBottomTab) replacing Arrow.js reactive pattern
- Converted all 5 framework-agnostic utility modules to TypeScript with proper type annotations
- Replaced all window.__TAURI__ global access with proper @tauri-apps/api imports across all files
- Added TypeScript interfaces: ProjectEntry, GitData, AppState, TerminalOptions, TerminalInstance, ThemeData, ChromeTheme, TerminalRegistration

## Task Commits

Each task was committed atomically:

1. **Task 1: Convert state-manager.js to signal-based state-manager.ts** - `ba7e0f7` (feat)
2. **Task 2: Convert drag-manager.js, terminal/*.js, and theme/*.js to TypeScript** - `d51eab7` (feat)

## Files Created
- `src/state-manager.ts` - Signal-based state management with Tauri IPC, 5 exported signals, 13 exported functions
- `src/drag-manager.ts` - Framework-agnostic DOM drag handler with typed callbacks
- `src/terminal/terminal-manager.ts` - xterm.js lifecycle with WebGL fallback, typed options and return value
- `src/terminal/pty-bridge.ts` - PTY Channel connection with @tauri-apps/api/core imports
- `src/terminal/resize-handler.ts` - ResizeObserver + FitAddon debounced IPC with typed params
- `src/theme/theme-manager.ts` - Theme lifecycle with hot-reload, ChromeTheme/ThemeData interfaces

## Decisions Made
- Kept currentState as plain mutable variable rather than a signal since it is the raw Rust state blob used for persistence, not UI-reactive rendering
- Maintained CustomEvent dispatch in switchProject for backward compatibility with main.js (to be removed in Plan 05)
- Used `import type` for xterm.js and addon imports in modules that only reference types, not runtime values

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All utility modules are now TypeScript with proper types and @tauri-apps/api imports
- Components in Plans 03-04 can import signals from state-manager.ts and functions from typed utility modules
- Old .js files intentionally preserved for Plan 05 cleanup

## Self-Check: PASSED

- All 6 .ts files exist at expected paths
- Commit ba7e0f7 (Task 1) found in git log
- Commit d51eab7 (Task 2) found in git log
- SUMMARY.md created at expected path

---
*Phase: 06.1-migrate-arrow-js-to-preact-htm-inserted*
*Completed: 2026-04-08*
