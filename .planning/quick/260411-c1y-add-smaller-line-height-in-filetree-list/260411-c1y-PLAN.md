---
phase: quick-260411-c1y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/file-tree.tsx
  - src/components/preferences-panel.tsx
autonomous: true
must_haves:
  truths:
    - "File tree entries display with tighter line height by default"
    - "Preferences panel has BG color control for file tree"
    - "All three file tree options (font size, line height, BG color) work in real-time"
  artifacts:
    - path: "src/components/file-tree.tsx"
      provides: "Tighter default line height, BG color signal"
    - path: "src/components/preferences-panel.tsx"
      provides: "BG color picker/selector for file tree section"
---

<objective>
Make the file tree list more compact by reducing default line height, and add a BG color control to preferences panel so all three file tree appearance options (font size, line height, BG color) are user-configurable.

Purpose: File tree currently has generous padding (5px) that wastes vertical space. Adding BG color control completes the file tree theming options.
Output: Tighter file tree, full theme control in preferences.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/file-tree.tsx
@src/components/preferences-panel.tsx
@src/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce default line height and add BG color control to preferences</name>
  <files>src/components/file-tree.tsx, src/components/preferences-panel.tsx</files>
  <action>
1. In `src/components/file-tree.tsx`: Change the default value of `fileTreeLineHeight` signal from `5` to `2` (tighter padding by default).

2. In `src/components/preferences-panel.tsx`:
   - Add `fileTreeBgColor` to the import from `./file-tree.tsx`
   - In the FILE TREE section of the preferences panel, after the existing Line Height slider, add a BG Color control:
     - Label: "BG Color"
     - Use a color input (`<input type="color">`) bound to `fileTreeBgColor.value`
     - Add a small reset button next to it (text "Reset" or an "x") that sets `fileTreeBgColor.value = ''` to revert to the default `bgDeep` color
     - When `fileTreeBgColor.value` is empty string, the color input should show the current `bgDeep` value from tokens as its display value
   - Import `colors` from `../tokens.ts` if not already imported, to read `bgDeep` for the default display value
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && pnpm exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - fileTreeLineHeight defaults to 2 (was 5) making entries more compact
  - Preferences panel shows BG Color picker in FILE TREE section
  - Reset button clears custom BG color back to default bgDeep
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes
- Open app, file tree shows tighter line spacing by default
- Preferences panel FILE TREE section has all 3 controls: Font Size, Line Height, BG Color
- Changing BG color in preferences immediately updates file tree background
- Reset button reverts to default background color
</verification>

<success_criteria>
File tree entries are visually more compact with 2px padding default. All three file tree appearance settings (font size, line height, BG color) are controllable from the preferences panel.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-c1y-add-smaller-line-height-in-filetree-list/260411-c1y-SUMMARY.md`
</output>
