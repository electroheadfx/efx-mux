---
phase: quick
plan: 260416-g36
type: execute
wave: 1
depends_on: []
files_modified: [src/components/git-control-tab.tsx]
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "History rows show commit message and relative time only -- no author name visible"
    - "Hovering a history row shows a tooltip with author name, full timestamp, and short hash"
  artifacts:
    - path: "src/components/git-control-tab.tsx"
      provides: "Updated HistoryEntry component"
      contains: "title="
  key_links: []
---

<objective>
Remove commit author names from the git history sidebar rows and surface them in a hover tooltip instead.

Purpose: Reclaim horizontal space in the narrow sidebar by removing the author name from each history row. The information is still accessible on hover.
Output: Modified HistoryEntry component in git-control-tab.tsx
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/git-control-tab.tsx

<interfaces>
From src/services/git-service.ts:
```typescript
export interface GitCommitEntry {
  hash: string;
  short_hash: string;
  message: string;
  author: string;
  timestamp: number;  // Unix epoch seconds
  refs: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move author name from visible row to tooltip in HistoryEntry</name>
  <files>src/components/git-control-tab.tsx</files>
  <action>
In the `HistoryEntry` component (around line 536), make these changes:

1. Add a `title` attribute to the outer `div` of HistoryEntry that shows the full commit info:
   - Format: `"{entry.author} -- {entry.short_hash} -- {full date/time from entry.timestamp}"`
   - Use `new Date(entry.timestamp * 1000).toLocaleString()` for the full date/time string

2. In the right-aligned author+time span (around line 585-593), remove `{entry.author}, ` so only `{relativeTime(entry.timestamp)}` remains. The span currently reads:
   ```
   {entry.author}, {relativeTime(entry.timestamp)}
   ```
   Change it to:
   ```
   {relativeTime(entry.timestamp)}
   ```

That is the entire change -- two edits within the HistoryEntry function.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - History rows display only commit message, ref badges, and relative time -- no author name text visible
    - Hovering any history row shows a tooltip with author, short hash, and full timestamp
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes with no errors
- Visual: history rows in the git sidebar no longer show author names inline
- Visual: hovering a history row shows tooltip with author, hash, and date
</verification>

<success_criteria>
Author names removed from git history sidebar row text; tooltip shows author + hash + full timestamp on hover.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-g36-remove-commit-names-from-git-history-sid/260416-g36-SUMMARY.md`
</output>
