---
phase: 22
plan: 14
type: execute
wave: 4
depends_on:
  - 22-06
  - 22-07
  - 22-08
  - 22-09
  - 22-10
  - 22-11
  - 22-12
  - 22-13
files_modified:
  - .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md
  - .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
gap_closure: true
requirements_addressed:
  - TABS-01
  - SPLIT-01
  - SPLIT-02
  - SPLIT-03
  - SPLIT-04
  - PREF-01
requirements: [TABS-01, SPLIT-01, SPLIT-02, SPLIT-03, SPLIT-04, PREF-01]

must_haves:
  truths:
    - "Every UAT test 1..18 has a fresh result entry (pass / issue / pass-with-notes) recorded after the gap-closure batch ships"
    - "Every previously-failing gap is re-tested and either marked CLOSED or carried forward as a known limitation"
    - "REQUIREMENTS.md TABS-01, SPLIT-01..04, PREF-01 status updated to Complete (or remaining gaps surfaced) — section is explicitly created if it does not yet exist"
    - "ROADMAP.md Phase 22 marked complete + plan count updated to include 22-06..22-14"
    - "Obsolete plan 22-05-PLAN.md is renamed with .superseded suffix and removed from ROADMAP's Phase 22 plan list"
  artifacts:
    - path: ".planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md"
      provides: "Updated test/gap entries reflecting gap-closure batch outcomes"
      contains: "## Tests"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TABS-01..PREF-01 status entries set to Complete (per closed UAT)"
      contains: "TABS-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 22 plan count updated; Phase 22 status set to Complete"
      contains: "Phase 22"
    - path: ".planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md.superseded"
      provides: "Marker that the original 22-05 plan was replaced by the gap-closure batch"
      contains: "superseded"
  key_links: []
---

<objective>
Re-run UAT and close out Phase 22.

Replaces obsolete plan 22-05 — that plan was a "verify and closeout" against the broken implementation; the implementation is now fixed by 22-06..22-13 and needs fresh verification.

Closes (validates closure of) all 8 UAT gaps + 11 issue-results from 22-UAT.md.

Purpose: Establish that the gap-closure batch achieved its goal; surface any residual issues that need a follow-on phase.
Output: Updated 22-UAT.md, REQUIREMENTS.md, ROADMAP.md, and the obsolete 22-05-PLAN.md superseded.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md
@.planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run all 18 UAT tests against the gap-closed build</name>
  <what-built>Phase 22 gap-closure batch (plans 22-06..22-13): cold-start fix, per-project split state, first-launch flag, per-mutation persistence, singleton labels + close + drag, closeSubScope helper, intra-zone resize repair, +-button scope routing, counter decision, editor reorder + drop fallback.</what-built>
  <how-to-verify>
Re-run every test in `.planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md` using its existing `expected:` script. For each:

1. **Cold Start Smoke** — Quit any running Efxmux. Launch fresh. Open devtools. Confirm ZERO `unknown terminal scope` errors AND zero unhandled promise rejections.

2. **First-Launch Defaults (New Project)** — Create a brand-new project that has never been opened. Verify: main panel shows 1 terminal labeled like the project name; right panel shows GSD + File Tree as 2 dynamic tabs (closeable); right panel is NOT pre-split into 2 panes.

3. **Titlebar Preferences Button** — already passed; spot-check still works.

4. **Click Preferences Button** — already passed; spot-check.

5. **Cmd+, Opens Preferences** — already passed; spot-check.

6. **Titlebar Drag** + the 4 sub-issues:
   - (a) Click a file in file tree → tab opens AND content renders without re-click ✓
   - (b) Drag editor tab within bar → reorders ✓
   - (c) Drag editor tab to empty pane → appears in target scope ✓
   - (d) Drop without visible insertion marker → appended at end (NOT rejected) ✓

7. **GSD Tab is Dynamic**:
   - (a) `+` menu GSD item creates GSD tab in originating scope
   - (b) GSD tab title displays "GSD" (NOT "Git Changes")
   - (c) GSD draggable to empty pane (no text-selection hijack)
   - (d) GSD draggable between two panes

8. **File Tree Tab is Dynamic**:
   - (a) Close button on File Tree tab actually removes it
   - (b) File Tree tab title displays "File Tree" (NOT "Git Changes")
   - (c) File Tree reorderable among terminal/TUI tabs

9. **Split Icon in Tab Bar**:
   - Resize handle drags actually move panes (test 12 closure)
   - Tab bar has visible top border (not just bottom)
   - Split icon has visible right margin

10. **Split Creates Second Sub-Scope** — already passed.

11. **Split Cap at 3** — already passed.

12. **Intra-Zone Resize Handle** — drag works; ratio persists across restart (test 13 unblocked).

13. **Split Ratio Persists on Restart** — formerly blocked; now testable.

14. **Cross-Scope Drag of GSD** — drag GSD from right to main; appears in main; right no longer shows GSD.

15. **Shared Tab Counter (Sequential Names)**:
    - (a) Counter behavior matches user's 22-12 decision (monotonic OR gap-fill)
    - (b) `+` clicked in right-1 spawns terminal in right-1 (NOT main-0)

16. **Tab Counter Persists Across Restart**:
    - Spawn 4 tabs → quit → reopen → 4 tabs present
    - Delete tab 2 → quit → reopen → tab 2 still gone (NOT auto-recreated)
    - Add new → counter continues from highest persisted

17. **Legacy State Migration** — skipped (no legacy state available); leave as skipped.

18. **Split-Pane Bodies Preserve State**:
    - Open editor + scroll → split → top pane preserved
    - Click "close split" on bottom pane → tabs migrate to top pane (closeSubScope ✓)

For each test, record one of:
- `pass` — works as expected
- `issue` — describe the residual problem
- `regression` — worked before, broken now (severity blocker)

Also exercise edge cases the user previously surfaced:
- Switch between 2 projects with different split configurations — confirm no leakage
- Split-resize a pane in project A → switch to project B (no split) → switch back to A → ratio restored
  </how-to-verify>
  <resume-signal>Reply with one of: `all-pass`, `issues-listed-below: <list>`, or `regression-found: <description>`</resume-signal>
  <read_first>
    - .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md (the 18 tests + 8 gaps)
  </read_first>
  <action>
The user runs through the 18 UAT tests above. The agent waits for the resume signal.

Once the user replies, the agent:
1. If `all-pass`: proceed to Task 2.
2. If `issues-listed-below`: surface each issue. If any are `blocker` severity, halt and recommend a follow-on gap-closure plan 22-15. Otherwise document in 22-UAT.md as residual + proceed to Task 2.
3. If `regression-found`: halt; recommend a hotfix plan 22-15 immediately; do NOT proceed to Task 2.
  </action>
  <acceptance_criteria>
    - User has replied with one of the resume signals
  </acceptance_criteria>
  <verify>
    <automated>echo "Awaiting human-verify reply"</automated>
  </verify>
  <done>User has reported UAT results.</done>
</task>

<task type="auto">
  <name>Task 2: Update 22-UAT.md with re-run results</name>
  <files>.planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md</files>
  <read_first>
    - .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md (current file)
  </read_first>
  <action>
1. Update the frontmatter `status:` from `partial` to `closed` (if all-pass) or `partial-with-residuals` (if some issues remain non-blocker).

2. Update each test's `result:` based on the user's UAT report. For tests that now pass:
   - Change `result: issue` → `result: pass`
   - Add a `closed_by: 22-06..22-13` annotation
   - Keep the original `reported:` text but add `re_tested: <date> – PASS`

3. Update the `## Gaps` section: for each closed gap, change `status: failed` → `status: closed` with `closed_by:` annotation citing the responsible plan(s).

4. Update the `## Summary`:
   ```
   total: 18
   passed: <new pass count>
   issues: <residual issues>
   pending: 0
   skipped: 1 (test 17, no legacy state available)
   blocked: 0
   ```

5. If any residuals remain, add a new section `## Residuals (carried forward)` listing them with proposed follow-on plan numbers (22-15+).

Commit: `test(22-14): UAT re-run after gap-closure batch — <new pass count>/18 passed`
  </action>
  <acceptance_criteria>
    - `grep -nE "^status:" .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md` shows updated status
    - `grep -cE "result: pass" .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md` is > 5 (the original count)
    - `grep -nE "closed_by:" .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md | wc -l` returns at least 4 (one per major closed gap)
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "result: pass" .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-UAT.md</automated>
  </verify>
  <done>22-UAT.md reflects the gap-closure outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Supersede 22-05; add Phase 22 section to REQUIREMENTS.md (creating it if absent); update ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find the Traceability table; check whether a "Workspace Shell (TABS / SPLIT / PREF)" or any TABS-01 / SPLIT-01..04 / PREF-01 entries already exist — currently they do NOT per pre-revision audit. The Phase 22 entries must be CREATED, not just edited.)
    - .planning/ROADMAP.md (find the Phase 22 entry around line 213; find the Phase 22 plans list including the obsolete `[ ] 22-05-PLAN.md` line)
    - ls .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md (confirm the file exists before renaming)
  </read_first>
  <action>
**(0) Supersede 22-05** — the original 22-05 plan ("verify and closeout against broken implementation") is obsolete. Rename the file with a `.superseded` suffix so it remains in git history but no longer matches the planner's `*-PLAN.md` glob:

```bash
mv .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md \
   .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md.superseded
```

(If the file is already missing or already renamed, this is a no-op — proceed.)

**(1) `.planning/REQUIREMENTS.md`** — section creation is EXPLICIT (not implicit):

First, check whether the section already exists:
```bash
grep -c "^### Workspace Shell" .planning/REQUIREMENTS.md
```

- **If the count is 0**: the section does NOT exist yet. Find the v0.3.0 marker (search for `## v0.3.0` or the Workspace Evolution heading near it). Insert a NEW section under v0.3.0:

  ```markdown
  ### Workspace Shell (TABS / SPLIT / PREF) — Phase 22

  - [x] **TABS-01**: Dynamic tabs with fixed titles for File Tree / GSD / Git Changes; closeable, drag-reorderable, addable via `+`; per-scope persisted across restart
  - [x] **SPLIT-01**: Main + right zones support 1-3 stacked sub-panes via split icon
  - [x] **SPLIT-02**: Singleton enforcement for GSD + Git Changes (`+` menu dimmed when singleton already open)
  - [x] **SPLIT-03**: All tab kinds drag freely between any two scopes
  - [x] **SPLIT-04**: PTY session names stable across drags via shared per-project counter
  - [x] **PREF-01**: Titlebar Preferences button (Settings gear) opens preferences panel; Cmd+, unchanged
  ```

- **If the count is >= 1**: the section already exists. Update the checkbox states to `[x]` and add any missing TABS-01/SPLIT-0x/PREF-01 bullets that are not yet present.

Then, regardless of which branch ran above, append the traceability rows to the Traceability table:

```markdown
| TABS-01 | Phase 22 | Complete |
| SPLIT-01 | Phase 22 | Complete |
| SPLIT-02 | Phase 22 | Complete |
| SPLIT-03 | Phase 22 | Complete |
| SPLIT-04 | Phase 22 | Complete |
| PREF-01 | Phase 22 | Complete |
```

(If any UAT residuals remain, set the corresponding row's status to `Partial` and add a footnote citing the residual.)

**(2) `.planning/ROADMAP.md`** — find the Phase 22 section (around line 213). Update:
- `**Plans:** 3/5 plans executed` → `**Plans:** 13/13 plans executed` (gap-closure batch is 22-06..22-14 = 9 new plans; original was 22-01..22-04 = 4 plans; 22-05 is being superseded so it does NOT count. Total: 4 + 9 = 13)
- Status `In Progress` → `Complete` in the progress table (line ~211)
- **REMOVE** the `[ ] 22-05-PLAN.md` line from the Plans list (it's been superseded by 22-14, which now also handles closeout)
- Add the new plan entries after `22-04`:
```markdown
  - [x] 22-06-PLAN.md — Gap closure: cold-start fix + main.tsx hierarchical scope migration
  - [x] 22-07-PLAN.md — Gap closure: per-project split state + first-launch flag
  - [x] 22-08-PLAN.md — Gap closure: per-mutation tab persistence + counter persist
  - [x] 22-09-PLAN.md — Gap closure: singleton labels + close + drag + empty-pane drop
  - [x] 22-10-PLAN.md — Gap closure: closeSubScope helper + close-split button + first-open file activation
  - [x] 22-11-PLAN.md — Gap closure: intra-zone resize repair + tab bar borders + split icon margin
  - [x] 22-12-PLAN.md — Gap closure: + button scope routing + counter behavior decision
  - [x] 22-13-PLAN.md — Gap closure: editor reorder + drop fallback append-last
  - [x] 22-14-PLAN.md — Gap closure: UAT re-run + REQUIREMENTS/ROADMAP closeout (supersedes 22-05)
```

Update the Last updated line:
```
*Last updated: <today> (Phase 22 closed via gap-closure batch 22-06..22-14; 22-05 superseded)*
```

Commit: `docs(22-14): close Phase 22 — REQUIREMENTS section created; ROADMAP updated; 22-05 superseded`
  </action>
  <acceptance_criteria>
    - `test ! -f .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md && test -f .planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-05-PLAN.md.superseded` succeeds (renamed)
    - `grep -c "^### Workspace Shell" .planning/REQUIREMENTS.md` returns at least 1 (section explicitly created or pre-existing — Nyquist-verified)
    - `grep -nE "TABS-01.*Phase 22.*Complete|TABS-01 \\| Phase 22 \\| Complete" .planning/REQUIREMENTS.md` matches at least 1
    - `grep -nE "SPLIT-01.*Complete|SPLIT-01 \\| Phase 22 \\| Complete" .planning/REQUIREMENTS.md` matches
    - `grep -nE "PREF-01.*Complete" .planning/REQUIREMENTS.md` matches
    - `grep -n "22-06-PLAN.md\\|22-14-PLAN.md" .planning/ROADMAP.md | wc -l` returns at least 2
    - `grep -nE "\\[ \\] 22-05-PLAN\\.md" .planning/ROADMAP.md | wc -l` returns 0 (the obsolete entry removed)
    - `grep -nE "22\\. Dynamic tabs.*Complete" .planning/ROADMAP.md` matches (Phase 22 status line in progress table)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^### Workspace Shell" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>22-05 superseded; REQUIREMENTS Phase 22 section created (or updated); ROADMAP reflects Phase 22 closeout without the obsolete 22-05 entry.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only plan.
</threat_model>

<verification>
- 22-UAT.md status reflects post-batch outcomes
- REQUIREMENTS.md TABS-01..PREF-01 marked Complete (or Partial w/ residual notes), with the section explicitly created if it was absent
- ROADMAP.md Phase 22 status marked Complete; plan count updated; obsolete 22-05 entry removed
- 22-05-PLAN.md renamed to 22-05-PLAN.md.superseded
- Any residuals listed under `## Residuals (carried forward)` with proposed follow-on plan numbers
</verification>

<success_criteria>
- All 18 UAT tests have fresh results
- Phase 22 closed in REQUIREMENTS + ROADMAP (or residuals explicitly captured)
- Obsolete 22-05 superseded and removed from ROADMAP plan list
- User has confirmed UAT outcomes
</success_criteria>

<output>
After completion, create `.planning/phases/22-dynamic-tabs-vertical-split-and-preferences-modal/22-14-SUMMARY.md` documenting:
- New UAT pass count vs original 5/18
- Any residual issues + their follow-on plan numbers
- Phase 22 closeout date + summary of the 9 gap-closure plans (22-06..22-14)
- Confirmation that 22-05 was superseded (and why)
- Confirmation that the REQUIREMENTS.md "Workspace Shell" section was created (or that it pre-existed and was just updated)
</output>
</content>
</invoke>