---
phase: 06.1-migrate-arrow-js-to-preact-htm-inserted
plan: 01
subsystem: ui
tags: [vite, preact, tailwind, typescript, vitest, build-toolchain]

# Dependency graph
requires: []
provides:
  - Vite + Preact + Tailwind 4 build toolchain
  - TypeScript config with Preact JSX support
  - Vitest test infrastructure with jsdom
  - Tailwind app.css with Solarized Dark @theme tokens
  - Vite-compatible index.html entry point
affects: [06.1-02, 06.1-03, 06.1-04, 06.1-05]

# Tech tracking
tech-stack:
  added: [preact, "@preact/signals", vite, "@preact/preset-vite", tailwindcss, "@tailwindcss/vite", typescript, vitest]
  patterns: [Tailwind 4 @theme tokens for design tokens, Vite dev server on port 1420, jsdom test environment]

key-files:
  created: [vite.config.ts, tsconfig.json, vitest.config.ts, src/styles/app.css]
  modified: [package.json, src/index.html]

key-decisions:
  - "tauri-plugin-dialog already present in Cargo.toml -- no changes needed to Rust side"
  - "Added jsdom as dev dependency for vitest environment"
  - "Kept data-theme attribute approach for light mode toggle (consistent with existing theme-manager)"

patterns-established:
  - "Tailwind @theme tokens: --color-bg, --color-bg-raised, --color-border, --color-text, --color-text-bright, --color-accent"
  - "Structural CSS (split handles, scrollbar overrides) lives in app.css, not Tailwind utilities"
  - "Vite entry: index.html -> /src/main.tsx"

requirements-completed: [PRESERVE-ALL, SC-02]

# Metrics
duration: 2min
completed: 2026-04-08
---

# Phase 6.1 Plan 01: Build Toolchain Setup Summary

**Vite + Preact + Tailwind 4 + TypeScript + Vitest build scaffold with Solarized Dark @theme tokens and Vite-compatible index.html entry point**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-08T13:48:52Z
- **Completed:** 2026-04-08T13:50:44Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Installed all npm dependencies: preact, @preact/signals, vite, @preact/preset-vite, tailwindcss, @tailwindcss/vite, typescript, vitest
- Created vite.config.ts, tsconfig.json, and vitest.config.ts with Preact + Tailwind + Tauri integration
- Created src/styles/app.css with Tailwind 4 @theme tokens matching D-12 Solarized Dark palette, plus all preserved structural CSS from layout.css
- Rewrote src/index.html as Vite entry point (removed importmap, references /src/main.tsx, title is "Efxmux")

## Task Commits

Each task was committed atomically:

1. **Task 1: Install npm dependencies and create build config files** - `52ba669` (feat)
2. **Task 2: Create Tailwind app.css and update index.html for Vite** - `6d3d313` (feat)

## Files Created/Modified
- `package.json` - Added runtime and dev dependencies, new scripts (dev, build, preview, test)
- `vite.config.ts` - Vite config with preact() and tailwindcss() plugins, port 1420, Tauri env prefix
- `tsconfig.json` - TypeScript strict mode with Preact JSX support (jsxImportSource: preact)
- `vitest.config.ts` - Vitest with jsdom environment and Preact plugin
- `src/styles/app.css` - Tailwind entry CSS with @theme tokens, font-face, light mode override, structural CSS
- `src/index.html` - Vite entry point with /src/main.tsx script, no importmap

## Decisions Made
- tauri-plugin-dialog was already present in Cargo.toml (added in Phase 6), no Rust-side changes needed
- Added jsdom as explicit dev dependency for vitest's jsdom environment
- Preserved structural CSS (split handles, scrollbar overrides, GSD content styles) in app.css rather than converting to Tailwind utilities, as they use CSS calc() with custom properties

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Build toolchain ready for component migration (Plans 02-04)
- main.tsx does not exist yet (created in Plan 05) -- Vite dev server will fail to start until then
- Old theme.css and layout.css preserved (removed in Plan 05 cleanup)

## Self-Check: PASSED

---
*Phase: 06.1-migrate-arrow-js-to-preact-htm-inserted*
*Completed: 2026-04-08*
