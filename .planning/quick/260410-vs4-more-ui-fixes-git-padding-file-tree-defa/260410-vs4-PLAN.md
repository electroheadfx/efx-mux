---
quick_id: 260410-vs4
slug: more-ui-fixes-git-padding-file-tree-defa
description: More UI fixes: git padding, file tree defaults, server pane BG, theme options, extra newlines
date: 2026-04-10
mode: quick
---

# Quick Plan: 260410-vs4

## Tasks

### 1. Git diff title padding: 1px 7px 4px
**File:** `src/components/diff-viewer.tsx`
**Action:** Change header padding from `10px 2px` to `1px 7px 4px`
**Done:** ✓

### 2. Git diff content padding: 4px all sides
**File:** `src/components/diff-viewer.tsx`
**Action:** Change all content line padding from `2px 2px` to `4px`
**Done:** ✓

### 3. File Tree pane not open by default
**File:** `src/state-manager.ts`
**Action:** Change `rightTopTab` default from `'File Tree'` to `'GSD'`
**Done:** ✓

### 4. Server pane background color
**File:** `src/styles/app.css`
**Action:** `.server-pane-logs` already has `background: var(--color-bg)` from previous fix. Verify it's correct.
**Done:** ✓

### 5. File tree theme options in theme.json
**Files:** `src-tauri/src/theme/types.rs`, `src/theme/theme-manager.ts`, `src/components/file-tree.tsx`
**Action:** Add fileTreeBg, fileTreeFont, fileTreeFontSize, fileTreeLineHeight to ChromeTheme + wire to signals
**Done:** ✓

### 6. Extra newlines in claude session on app open
**Status:** Root cause in pty.rs shell wrapping (`clear` + newlines from shell init). Requires deeper investigation.
**Deferred:** Complex - shell init behavior
