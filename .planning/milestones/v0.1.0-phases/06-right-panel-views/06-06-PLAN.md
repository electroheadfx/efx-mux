---
phase: 06-right-panel-views
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/diff-viewer.js
autonomous: true
gap_closure: true
requirements:
  - PANEL-04

must_haves:
  truths:
    - "Diff Viewer renders syntax-highlighted unified diffs when a file is clicked in the sidebar"
    - "contentEl is assigned via getElementById, not an Arrow.js ref callback"
  artifacts:
    - path: "src/components/diff-viewer.js"
      provides: "Git diff renderer with working DOM reference"
      contains: "getElementById"
  key_links:
    - from: "src/components/diff-viewer.js"
      to: "document.getElementById"
      via: "setTimeout + getElementById pattern"
      pattern: "getElementById.*diff-viewer-content"
---

<objective>
Fix the Arrow.js ref callback bug in diff-viewer.js that prevents diffs from rendering.

Purpose: diff-viewer.js line 94 uses `ref="${(el) => { contentEl = el; }}"` which Arrow.js serializes as a string attribute -- contentEl is never assigned, so loadDiff() exits early at line 63. This is the last remaining gap in Phase 6 verification (PANEL-04).

Output: Working diff-viewer.js where contentEl is resolved via setTimeout + getElementById, matching the proven pattern in gsd-viewer.js lines 114-121.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/diff-viewer.js
@src/components/gsd-viewer.js

<interfaces>
<!-- The fix pattern from gsd-viewer.js lines 114-121 (the reference implementation): -->

```javascript
// Schedule DOM discovery after Arrow.js renders (06-04 pattern: getElementById, not ref)
setTimeout(() => {
  contentEl = document.getElementById('gsd-viewer-content');
  if (contentEl) {
    const project = activeProject();
    if (project) loadGSD(project);
  }
}, 0);
```

<!-- In diff-viewer.js, the equivalent post-mount action is simpler: no initial load needed,
     just assign contentEl so loadDiff() works when open-diff events arrive. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix diff-viewer.js ref callback with getElementById pattern</name>
  <files>src/components/diff-viewer.js</files>
  <read_first>
    - src/components/diff-viewer.js (current broken state -- see line 94 ref callback, line 56 contentEl declaration, line 63 early return)
    - src/components/gsd-viewer.js (reference fix pattern -- lines 114-121 setTimeout + getElementById)
  </read_first>
  <action>
Two changes in src/components/diff-viewer.js:

1. **Line 94**: Replace `ref="${(el) => { contentEl = el; }}"` with `id="diff-viewer-content"`.
   Before: `ref="${(el) => { contentEl = el; }}"`
   After:  `id="diff-viewer-content"`

2. **After the event listener block (after line 79) and before the return statement (line 81)**: Add the setTimeout + getElementById pattern to resolve contentEl after Arrow.js renders:

```javascript
  // Schedule DOM discovery after Arrow.js renders (same pattern as gsd-viewer.js)
  setTimeout(() => {
    contentEl = document.getElementById('diff-viewer-content');
  }, 0);
```

This is placed between the event listener registration (line 75-79) and the `return html` statement (line 81). No initial load is needed unlike gsd-viewer -- diff-viewer only loads on open-diff events, so assigning contentEl is sufficient.
  </action>
  <verify>
    <automated>grep -n 'getElementById.*diff-viewer-content' src/components/diff-viewer.js && grep -n 'id="diff-viewer-content"' src/components/diff-viewer.js && ! grep -n 'ref="' src/components/diff-viewer.js && echo "PASS: ref removed, getElementById added"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'ref="' src/components/diff-viewer.js returns 0 matches (no ref callbacks remain)
    - grep 'getElementById.*diff-viewer-content' src/components/diff-viewer.js returns exactly 1 match
    - grep 'id="diff-viewer-content"' src/components/diff-viewer.js returns exactly 1 match
    - grep 'setTimeout' src/components/diff-viewer.js returns exactly 1 match
    - The file still contains the loadDiff function, renderDiffHtml function, open-diff listener, and escapeHtml function (no regressions)
  </acceptance_criteria>
  <done>contentEl is assigned via getElementById after Arrow.js render. loadDiff() no longer exits early. Diffs render when sidebar file is clicked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git diff output -> innerHTML | Diff text from git2 backend rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | S (Spoofing) | diff-viewer.js innerHTML | accept | Already mitigated: escapeHtml() at line 13-19 sanitizes all diff text before innerHTML assignment. No change in this plan. |
</threat_model>

<verification>
1. `grep -c 'ref="' src/components/diff-viewer.js` returns `0`
2. `grep -c 'getElementById' src/components/diff-viewer.js` returns `1`
3. `grep -c 'diff-viewer-content' src/components/diff-viewer.js` returns `2` (one in getElementById, one in id attribute)
4. `cargo check` in src-tauri/ still passes (no Rust changes, but sanity check)
</verification>

<success_criteria>
- diff-viewer.js uses getElementById('diff-viewer-content') instead of Arrow.js ref callback
- No `ref="` pattern remains in diff-viewer.js
- PANEL-04 verification gap is closed: Diff Viewer can render syntax-highlighted unified diffs from git2
</success_criteria>

<output>
After completion, create `.planning/phases/06-right-panel-views/06-06-SUMMARY.md`
</output>
