---
phase: 09-professional-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar.tsx
  - src/components/tab-bar.tsx
  - package.json
autonomous: true
requirements: [UI-03, UI-06]
must_haves:
  truths:
    - "Sidebar project cards show green status dot for active project, gray outline for inactive"
    - "Sidebar project cards show git branch in a tinted badge with GitBranch icon"
    - "Git status badges show M (blue tint), S (green tint), U (orange tint) with colored background"
    - "Sidebar section headers use section-label class (uppercase Geist Mono 10px)"
    - "Tab bars use new palette colors for active pill state"
    - "lucide-preact is installed and icons render in sidebar"
  artifacts:
    - path: "src/components/sidebar.tsx"
      provides: "Rebuilt project cards, git status badges, Lucide icons"
      contains: "import { Circle, GitBranch"
    - path: "src/components/tab-bar.tsx"
      provides: "Updated pill colors using new palette"
      contains: "bg-bg-raised"
    - path: "package.json"
      provides: "lucide-preact dependency"
      contains: "lucide-preact"
  key_links:
    - from: "src/components/sidebar.tsx"
      to: "lucide-preact"
      via: "import"
      pattern: "from 'lucide-preact'"
---

<objective>
Install lucide-preact and rebuild the sidebar project cards and git status to match the Pencil mockup. Update tab bar pill colors to the new palette. This is the sidebar visual transformation.

Purpose: The sidebar is the most visible component -- its professional appearance sets the quality bar for the entire app.
Output: Restyled sidebar with Lucide icons, status dots, git branch badges, colored file status badges. Updated tab bar colors.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-professional-ui-overhaul/09-CONTEXT.md
@.planning/phases/09-professional-ui-overhaul/09-RESEARCH.md

<interfaces>
From src/state-manager.ts:
```typescript
export interface ProjectEntry {
  path: string;
  name: string;
  agent: string;
  gsd_file?: string;
  server_cmd?: string;
  server_url?: string;
}

export interface GitData {
  branch: string;
  modified: number;
  staged: number;
  untracked: number;
}

export const projects = signal<ProjectEntry[]>([]);
export const activeProjectName = signal<string | null>(null);
export const sidebarCollapsed = signal(false);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-preact and rebuild sidebar with mockup-matching design (D-09, D-10, D-12)</name>
  <files>src/components/sidebar.tsx, package.json</files>
  <read_first>
    - src/components/sidebar.tsx (full file -- current ProjectRow, GitFileRow, CollapsedIcon, Sidebar components)
    - package.json (current dependencies to verify lucide-preact not yet installed)
    - .planning/phases/09-professional-ui-overhaul/09-CONTEXT.md (D-09, D-10, D-12 decisions)
  </read_first>
  <action>
1. Install lucide-preact: `pnpm add lucide-preact`

2. Use the Pencil MCP tool `get_screenshot` with node ID `JtCpP` to view the Main App Layout mockup. Study the sidebar design carefully for exact layout, spacing, and visual hierarchy.

3. Rebuild sidebar.tsx -- keep ALL existing logic (signals, data fetching, event handlers, useEffect hooks). Only change the JSX rendering and imports. Key changes:

   a. Add imports at top:
   ```tsx
   import { Circle, GitBranch, Plus, RotateCw, X } from 'lucide-preact';
   ```

   b. Rebuild ProjectRow component per D-09:
   - Replace border-left active indicator with a Circle status dot (size=8)
   - Active project: `<Circle size={8} fill="currentColor" class="text-success shrink-0" />` (green filled dot)
   - Inactive project: `<Circle size={8} class="text-border-interactive shrink-0" />` (gray outline dot)
   - Project name: `text-sm font-medium text-text-bright` (Geist via font-sans inheritance)
   - Project path below name: `text-[11px] text-text truncate`
   - Git branch badge: pill with `flex items-center gap-1 text-[11px] text-accent px-1.5 py-0.5 bg-accent/10 rounded` containing `<GitBranch size={10} />` and branch text
   - Whole row: `flex items-center gap-2.5 px-3 py-2 rounded-md cursor-pointer` with active=`bg-bg-raised` inactive=`hover:bg-bg-raised/50`
   - Remove the old border-l-[3px] accent indicator pattern entirely

   c. Rebuild GitFileRow per D-09 colored badges:
   - Remove inline `style={{ color }}` completely
   - Replace with themed badge approach:
     - M (modified): `<span class="text-[10px] font-mono font-semibold px-1.5 py-0.5 rounded bg-accent/15 text-accent">M</span>`
     - S (staged): `<span class="text-[10px] font-mono font-semibold px-1.5 py-0.5 rounded bg-success/15 text-success">S</span>`
     - U (untracked): `<span class="text-[10px] font-mono font-semibold px-1.5 py-0.5 rounded bg-warning/15 text-warning">U</span>`
   - File name uses `text-xs text-text` with truncation

   d. Update git status summary badges (lines ~344-356) -- replace inline `style={{ color: '#b58900' }}` etc:
   - M count: `<span class="text-[11px] font-mono px-1.5 py-0.5 rounded bg-accent/15 text-accent">M {count}</span>`
   - S count: `<span class="text-[11px] font-mono px-1.5 py-0.5 rounded bg-success/15 text-success">S {count}</span>`
   - U count: `<span class="text-[11px] font-mono px-1.5 py-0.5 rounded bg-warning/15 text-warning">U {count}</span>`

   e. Section headers per D-10 -- replace inline uppercase text with section-label class:
   - "PROJECTS" header: `<div class="section-label pb-1">Projects</div>`
   - "GIT CHANGES" header: `<div class="section-label pb-1 flex-1">Git Changes</div>`
   - "EFXMUX" brand at top: keep as-is or use section-label

   f. Replace `+` text button with `<Plus size={14} class="text-text" />` for Add Project
   g. Replace `\u21BB` (refresh icon) with `<RotateCw size={12} class="text-text" />`
   h. Replace `\u2715` (X) remove button with `<X size={12} />`

   i. Update RemoveDialog: replace hardcoded `bg-[#dc322f]` with `bg-danger` on Remove button

   j. CollapsedIcon: add Circle dot indicator -- active projects get a small green dot below the initial letter

4. CRITICAL: Remove ALL inline `style={{ color: '#...' }}` patterns from sidebar.tsx. Every color must come from Tailwind theme tokens.
  </action>
  <verify>
    <automated>grep -c "lucide-preact" package.json && grep -c "from 'lucide-preact'" src/components/sidebar.tsx && ! grep -q "style={{.*color:" src/components/sidebar.tsx && echo "no-inline-colors-ok" && grep -c "section-label" src/components/sidebar.tsx && grep -c "bg-accent/15" src/components/sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"lucide-preact"` in dependencies
    - sidebar.tsx imports from 'lucide-preact' (Circle, GitBranch, Plus, RotateCw, X)
    - sidebar.tsx contains NO `style={{ color:` inline color declarations
    - sidebar.tsx contains NO hardcoded hex colors like `#b58900`, `#859900`, `#6c7b83`, `#dc322f`
    - sidebar.tsx uses `section-label` class on section headers
    - sidebar.tsx uses `bg-accent/15 text-accent` for M badge
    - sidebar.tsx uses `bg-success/15 text-success` for S badge
    - sidebar.tsx uses `bg-warning/15 text-warning` for U badge
    - sidebar.tsx uses `<Circle` component for status dots
    - sidebar.tsx uses `<GitBranch` component for branch badge
    - sidebar.tsx uses `bg-danger` instead of `bg-[#dc322f]`
  </acceptance_criteria>
  <done>Sidebar fully restyled with Lucide icons, status dots, git branch badges, colored file status badges using theme tokens. Zero inline color styles remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update tab bar pill styling to new palette (D-16)</name>
  <files>src/components/tab-bar.tsx</files>
  <read_first>
    - src/components/tab-bar.tsx (current pill-style active states, Tailwind classes)
  </read_first>
  <action>
Update TabBar component Tailwind classes to use new palette per D-16. The current component already uses rounded-full pills -- only the colors change.

Active tab button classes:
```
px-3 py-1 text-xs cursor-pointer font-sans bg-bg-raised text-text-bright rounded-full border border-border-interactive transition-all duration-150
```

Inactive tab button classes:
```
px-3 py-1 text-xs cursor-pointer font-sans bg-transparent text-text rounded-full border border-transparent transition-all duration-150 hover:text-text-bright hover:bg-bg-raised/40
```

Key changes from current:
- Replace `font-[inherit]` with `font-sans` (uses Geist via --font-family-sans)
- Active state: `border-border` -> `border-border-interactive` (D-01 introduces interactive border)
- Everything else stays the same -- the bg-bg-raised and text colors already use theme tokens

Also update the tab bar container:
```
flex gap-1 px-2 py-1.5 bg-bg border-b border-border shrink-0 items-center
```
This is unchanged -- it already uses theme tokens.
  </action>
  <verify>
    <automated>grep -c "font-sans" src/components/tab-bar.tsx && grep -c "border-border-interactive" src/components/tab-bar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tab-bar.tsx active state contains `border-border-interactive`
    - tab-bar.tsx uses `font-sans` instead of `font-[inherit]`
    - tab-bar.tsx still uses `rounded-full` for pill shape
    - tab-bar.tsx still uses `bg-bg-raised` for active background
  </acceptance_criteria>
  <done>Tab bar pill styling updated to use new palette colors with interactive border distinction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lucide-preact | Third-party npm package -- SVG icons rendered in DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | lucide-preact SVG output | accept | Lucide is a well-maintained OSS icon library (>46K GitHub stars), icons are static SVG, no dynamic content |
| T-09-04 | Tampering | sidebar git file names | mitigate | File names displayed via Preact text nodes (auto-escaped), no dangerouslySetInnerHTML |
</threat_model>

<verification>
1. `pnpm dev` runs without import errors
2. Sidebar shows Lucide icons (Circle dots, GitBranch, Plus, RotateCw)
3. Active project has green filled dot, inactive has gray outline
4. Git branch shows in pill-shaped badge with GitBranch icon
5. M/S/U badges have colored tinted backgrounds
6. Tab bars use new colors (check right panel tabs)
</verification>

<success_criteria>
- lucide-preact installed and tree-shaking individual icons
- Sidebar matches Pencil mockup layout for project cards
- All hardcoded solarized colors removed from sidebar
- Tab bar pills use interactive border on active state
</success_criteria>

<output>
After completion, create `.planning/phases/09-professional-ui-overhaul/09-02-SUMMARY.md`
</output>
