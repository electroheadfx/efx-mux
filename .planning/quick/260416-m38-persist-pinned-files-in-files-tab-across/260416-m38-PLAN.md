---
phase: quick
plan: 260416-m38
type: execute
wave: 1
depends_on: []
files_modified: [src/main.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pinned editor tabs survive Cmd+Q quit"
    - "Pinned editor tabs survive window X close"
    - "State is flushed to disk before process exits"
  artifacts:
    - path: "src/main.tsx"
      provides: "Pre-quit state save in both quit paths"
      contains: "saveAppState"
  key_links:
    - from: "src/main.tsx onConfirm"
      to: "state-manager.ts saveAppState"
      via: "await saveAppState before force_quit"
      pattern: "saveAppState.*force_quit"
---

<objective>
Fix pinned editor tabs lost on quit. The `force_quit` Tauri command calls `app_handle.exit(0)` which kills the process before `beforeunload` can fire, so `state.json` never gets the latest editor tab data. The fix: flush state to disk via `saveAppState` before calling `force_quit` in both quit paths (window close button and Cmd+Q menu).

Purpose: Editor tab state (including pinned files) must persist across application restarts.
Output: Patched `src/main.tsx` with pre-quit state save.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-mux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main.tsx
@src/state-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flush state to disk before force_quit in both quit paths</name>
  <files>src/main.tsx</files>
  <action>
In `src/main.tsx`, modify both `onConfirm` callbacks to save state before quitting:

1. **Line 126 (window X close path):** Change `onConfirm: () => { invoke('force_quit'); }` to:
   ```ts
   onConfirm: async () => {
     const state = getCurrentState();
     if (state) await saveAppState(state);
     invoke('force_quit');
   },
   ```

2. **Line 137 (Cmd+Q / menu quit path):** Apply the identical change:
   ```ts
   onConfirm: async () => {
     const state = getCurrentState();
     if (state) await saveAppState(state);
     invoke('force_quit');
   },
   ```

Both `saveAppState` and `getCurrentState` are already imported from `state-manager` on line 30. No new imports needed.

`saveAppState` invokes the Rust `save_state` command which uses `spawn_blocking` for synchronous file I/O, so the write completes before `force_quit` runs `exit(0)`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-mux && grep -n "saveAppState" src/main.tsx | grep -c "onConfirm" || grep -A2 "onConfirm" src/main.tsx | grep -c "saveAppState"</automated>
  </verify>
  <done>Both quit confirmation callbacks (window close + Cmd+Q) call saveAppState before force_quit. Grep confirms saveAppState appears in the onConfirm blocks.</done>
</task>

</tasks>

<verification>
1. `grep -A3 'onConfirm' src/main.tsx` shows `saveAppState` before `force_quit` in both callbacks
2. Manual: Open files in editor tabs, pin some, quit via Cmd+Q, relaunch -- pinned tabs restored
3. Manual: Same test via window X close button
</verification>

<success_criteria>
- Both quit paths flush state before exit
- No regression: quit still works (no hang on the await)
- Editor tabs (including pinned) persist across quit/relaunch
</success_criteria>

<output>
After completion, create `.planning/quick/260416-m38-persist-pinned-files-in-files-tab-across/260416-m38-SUMMARY.md`
</output>
