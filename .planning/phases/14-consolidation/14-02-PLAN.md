---
wave: 1
depends_on: []
requirements_addressed: [CONS-03]
files_modified:
  - package.json
  - src-tauri/Cargo.toml
  - CLAUDE.md
autonomous: true
---

<objective>
Audit dependencies in package.json and Cargo.toml: verify all packages/crates are actually imported in source code, check version alignment against CLAUDE.md version matrix, and remove unused dependencies. Rust side test (cargo test) must still pass after any removals.
</objective>

<tasks>

<task>
<read_first>
- package.json
- CLAUDE.md §"Version Matrix"
- src/main.tsx
- src/tokens.ts
</read_first>
<action>
Verify all package.json dependencies are actually imported somewhere in src/:

1. Extract dependency list from package.json (exclude devDependencies for now):
   - @preact/signals, @tauri-apps/api, @tauri-apps/plugin-dialog, @tauri-apps/plugin-opener
   - @xterm/xterm, @xterm/addon-fit, @xterm/addon-webgl, @xterm/addon-web-links
   - lucide-preact, marked, preact

2. For each dependency, grep for imports in src/:
   ```
   grep -r "from '@preact/signals'" src/
   grep -r "from '@tauri-apps/api'" src/
   grep -r "from '@tauri-apps/plugin-dialog'" src/
   grep -r "from '@tauri-apps/plugin-opener'" src/
   grep -r "from '@xterm/xterm'" src/
   grep -r "from '@xterm/addon-fit'" src/
   grep -r "from '@xterm/addon-webgl'" src/
   grep -r "from '@xterm/addon-web-links'" src/
   grep -r "from 'lucide-preact'" src/
   grep -r "from 'marked'" src/
   grep -r "from 'preact'" src/
   ```

3. For any dependency with zero import matches: flag for potential removal (verify via grep -r "package-name" src/ to rule out re-exports)

4. Report findings in: .planning/phases/14-consolidation/dep-audit-report.md
</action>
<acceptance_criteria>
- dep-audit-report.md exists listing each package and whether it's imported
- All actually-used packages remain in package.json
- No package is removed that is transitively imported
</acceptance_criteria>
</task>

<task>
<read_first>
- src-tauri/Cargo.toml
- CLAUDE.md §"Version Matrix"
- src-tauri/src/state.rs
- src-tauri/src/git_status.rs
- src-tauri/src/file_ops.rs
- src-tauri/src/server.rs
</read_first>
<action>
Verify all Cargo.toml crates are actually used in src-tauri/src/:

1. List crates from [dependencies] section:
   - tauri, tauri-plugin-opener, tauri-plugin-dialog, serde, serde_json, portable-pty, notify, notify-debouncer-mini, git2, regex, libc

2. For each crate, grep for its usage in src-tauri/src/:
   ```
   grep -l "use tauri" src-tauri/src/*.rs
   grep -l "use serde" src-tauri/src/*.rs
   grep -l "use portable_pty" src-tauri/src/*.rs
   grep -l "use notify" src-tauri/src/*.rs
   grep -l "use git2" src-tauri/src/*.rs
   grep -l "use regex" src-tauri/src/*.rs
   ```

3. Check Cargo.toml features for additional crate references:
   - tauri = { version = "2", features = [] } — features: [] means no feature flags

4. For any crate with no direct `use` statements: check if it's used via trait bounds or type references (e.g., serde used via #[derive(Serialize, Deserialize)])

5. Report findings in dep-audit-report.md Rust section
</action>
<acceptance_criteria>
- dep-audit-report.md lists each crate and its usage patterns in src-tauri/src/
- No crate has zero usage patterns (serde via derive counts as usage)
- dev-dependencies section (tempfile) is appropriate for testing
</acceptance_criteria>
</task>

<task>
<read_first>
- package.json
- Cargo.toml
- CLAUDE.md §"Version Matrix"
</read_first>
<action>
Compare actual package versions against CLAUDE.md version matrix:

For package.json, extract versions and compare to matrix:
```
node -e "const p=require('./package.json'); Object.entries(p.dependencies).forEach(([k,v])=>console.log(k,v))"
```

Version matrix from CLAUDE.md:
| tauri (Rust) | 2.10.3 | docs.rs confirmed | HIGH |
| @tauri-apps/api (JS) | ^2.0.0 | matches tauri core | HIGH |
| @xterm/xterm | 6.0.0 | GitHub releases confirmed | HIGH |
| @xterm/addon-webgl | 0.19.0 | npm confirmed | HIGH |
| @xterm/addon-fit | 0.11.0 | npm confirmed | HIGH |
| @xterm/addon-web-links | 0.12.0 | npm confirmed | HIGH |
| portable-pty (Rust) | 0.9.0 | docs.rs confirmed | HIGH |
| git2 (Rust) | 0.20.4 | docs.rs confirmed | HIGH |
| notify (Rust) | 8.2.0 | crates.io confirmed | HIGH |

Flag any discrepancies as "needs review" — versions may have been updated since matrix was written.
</action>
<acceptance_criteria>
- dep-audit-report.md has a version comparison section
- Discrepancies are flagged but NOT auto-fixed (manual review required)
- Matrix updates are NOT made unless explicitly requested by user
</acceptance_criteria>
</task>

<task>
<read_first>
- src-tauri/src/state.rs
- src-tauri/src/git_status.rs
- src-tauri/src/file_ops.rs
</read_first>
<action>
Run Rust test suite to confirm no regressions from any dependency audit findings:

1. `cd src-tauri && cargo test 2>&1` — run all Rust tests

If tests pass: no dependency issues found.

If tests fail: investigate whether cargo test was failing before consolidation (baseline check — this phase's changes haven't touched Rust source yet).
</action>
<acceptance_criteria>
- `cargo test` exits with code 0 in src-tauri/
- If tests fail, reason is identified and documented (not auto-fixed unless cause is dependency-related)
</acceptance_criteria>
</task>

</tasks>

<verification>
- dep-audit-report.md exists with full audit results
- No package removed from package.json (flag for review only)
- No crate removed from Cargo.toml (flag for review only)
- `cargo test` passes
- Version discrepancies are documented, not auto-fixed
</verification>

<must_haves>
1. dep-audit-report.md documents package.json audit results
2. dep-audit-report.md documents Cargo.toml audit results
3. dep-audit-report.md has version comparison section
4. `cargo test` passes (baseline intact)
5. No dependencies auto-removed — findings presented for manual review
</must_haves>